> [Project]: spec-driven AI coding loop.
> Current state: **Eighty-third roam-mode pass complete.** Coverage sweep — `cmd/drainctl` package first test file: `cmd/drainctl/notify_cmd_test.go` (18 tests) covering 4 pure helper functions that had been at 0% coverage since the package was first split (Roam #48). (1) `TestParseTriggers_*` (6 tests) — valid comma-separated list, single trigger, whitespace trimming, adjacent-comma empty-part skipping, unknown trigger error, all-empty error; `parseTriggers` 0% → 100%. (2) `TestTriggerList_*` (2 tests) — all 6 `ValidTriggers` appear in output, output is lexicographically sorted (deterministic help text); `triggerList` 0% → 100%. (3) `TestUpsertNotifyTarget_*` (4 tests) — updates URL of existing matching type, appends when no match, preserves other types, new target carries `DefaultTriggers`; `upsertNotifyTarget` 0% → 100%. (4) `TestPrintNotifyTargets_*` (6 tests) — empty targets → "notifications=disabled" warning, webhook with secret → `hmac_secret=set`, webhook without secret → `hmac_secret=unset`, ntfy target → no hmac_secret field, empty Triggers → `(default)` note, hasTargets=false → disabled message; `printNotifyTargets` 0% → 100%. `logCapture` helper implements `dc.LogFunc` signature (`func(dc.Level, ...string)`) inline in the test file. `cmd/drainctl` package: 0.0% → 7.5%. All tests pass, lint clean.

> Previous state: **Eighty-second roam-mode pass complete.** Coverage sweep + correctness fix: (1) `internal/dashboard/tls_test.go` (new file, 7 tests) — `generateSelfSigned`, `CertFingerprint`, and `loadOrGenerateTLS` were entirely at 0% coverage; tests cover the cert-write-error path (directory blocks cert file → `"write cert file"` error), `CertFingerprint` with no file (error), invalid PEM content (`"no PEM block"` error), invalid DER bytes (`"parse cert"` error), and the happy paths (skipped when not elevated — `writeRestrictedFile` strips key access to SYSTEM+Admins only, making the key unreadable in a non-elevated token); `isElevated()` helper uses `windows.Token.IsElevated()` to skip tests requiring admin; dashboard 49.7% → 52.6%. (2) `internal/svc/mgmt_test.go` (new file, 2 tests) — `svcStateString` was at 0% coverage despite being a pure function with no SCM dependency; `TestSvcStateString_AllNamedStates` covers all 7 named `svc.State` constants as sub-tests; `TestSvcStateString_UnknownStateUsesNumericFallback` covers the `default: "Unknown(N)"` branch; `svcStateString` 0% → 100%; svc package 8.1% → 10.3%. (3) `internal/store/memstore_test.go` — 3 new tests: `TestLoad_EmptyLineSkipped` — `if len(line) == 0 { continue }` branch in `load` was untested (existing scanner-error test jumped over it); writes a file with a blank line between two valid JSONL records and asserts both are loaded; `load` 76.9% → 84.6%. `TestFlushLocked_SeekError` — `m.file.Seek(0, 2)` error-return path in `flushLocked`; closing the OS file handle behind the store then calling `Flush()` produces the error; `flushLocked` 81.2% → 87.5%. `TestPrune_SeekError` — `m.file.Seek(0, 0)` error-return path in `Prune`; same file-close trigger with an old record already in memory (so `pruned > 0` is satisfied before the seek); `Prune` 82.1% → 89.3%; store 90.3% → 92.5%. (4) `SetRecoveryActions` correctness fix — `_ = s.SetRecoveryActions(...)` silently discarded errors in `installServiceImpl`; if the call fails the service will not auto-restart on crash; changed to log a `LvlWRN` message: `set_recovery_actions_failed=<err> (service will not auto-restart on crash)`. All tests pass, lint clean.

> Previous state: **Eighty-first roam-mode pass complete.** Security coverage + dead-code fix: (1) `internal/dashboard/client_test.go` (new file, 12 tests) — `newDashClient`'s `VerifyPeerCertificate` callback was security-critical code at 0% coverage; tests call the callback directly with in-memory self-signed certificates; covers empty-cert-list error, fingerprint-mismatch error (message includes both actual and expected values), correct-fingerprint success, TOFU mode (no callback when fingerprint is empty), leaf-only check when multiple certs are presented, and `InitDashClient` atomic replacement; `newDashClient` 27.3% → 100%, `init`/`InitDashClient` also reach 100%; dashboard 48.4% → 49.7%. (2) `backoffDuration` dead-code fix — `d > configFetchMax` was unreachable with `configFetchBase=5min` and shift capped at 5 (`d` reaches exactly `configFetchMax`, never exceeds it); changed `>` to `>=`; added `{-1, 5min}`, `{1000, 160min}`, and `TestBackoffDuration_NeverExceedsMax` invariant test; `backoffDuration` 88.9% → 100%. All tests pass, lint clean.

Previous state: **Seventy-ninth roam-mode pass complete.** Coverage sweep — 4 new tests across 2 files: (1) `TestScanRecords_EarlyStop` — `if !fn(rec) { break }` early-stop path in `scanRecords` was never exercised because all production callers return true from their callbacks; direct white-box test passes a callback that returns false after the first record; `scanRecords` 94.7% → 100%. (2) `TestFlushIfDirty_FlushesWhenDirty` — `return m.Flush()` path in `FlushIfDirty` was untested (only the `dirty==0` no-op path existed); append one record then call `FlushIfDirty`; verify file grows; `FlushIfDirty` 83.3% → 100%. (3) `TestClose_IdempotentOnSecondCall` — `if m.file == nil { return nil }` guard in `Close` was untested; call Close twice on the same store; `Close` 87.5% → 100%. (4) `TestOpenMemAuditStore_LoadError_OversizedLine` — `return nil, fmt.Errorf("load audit file: %w", err)` path in `OpenMemAuditStore` was untested; write a >64 KiB line to trigger `bufio.ErrTooLong` inside `load`; covers both the `load` scanner-error return and the `OpenMemAuditStore` load-failure return; `OpenMemAuditStore` 66.7% → 77.8%, `load` 76.9% (scanner-error now hit). Root package 55.6% → 55.7%; store 85.8% → 88.8%. All tests pass, lint clean.

Previous state: **Seventy-eighth roam-mode pass complete.** Coverage sweep — 3 new tests across 2 files: (1) `TestValidate_InvalidURLSchemeLogsWarning` — `log != nil` warn path in `Config.Validate` when a notification target carries an invalid URL scheme (ftp:// etc.) and a non-nil log is supplied; `Validate` 97.7% → 100%. (2) `TestDefaultDataDir_FallsBackWhenProgramDataEmpty` — `pd = "C:\ProgramData"` fallback in `DefaultDataDir` when `ProgramData` env var is unset (`t.Setenv("ProgramData","")` restores automatically); `DefaultDataDir` 75.0% → 100%. (3) `TestPrune_RenameError` — `os.Rename` failure in `AuditStore.Prune` exercised via `windows.CreateFile` with no `FILE_SHARE_DELETE` on the destination file so `MoveFileEx` returns a sharing violation; covers the rename error path and `os.Remove(tmp)` cleanup call; `Prune` 76.5% → 82.4%. Root: 55.2% → 55.6%. All tests pass, lint clean.

Previous state: **Seventy-seventh roam-mode pass complete.** Coverage sweep — 6 new tests across 2 files targeting previously-uncovered error branches: (1) `TestHistoryFiltered_ScanError` — `return nil, err` branch in `HistoryFiltered` when `scanRecords` fails (>64 KiB line triggers `bufio.ErrTooLong`); `HistoryFiltered` 95.2% → 100%. (2) `TestChangesFiltered_ScanError` — same scanner-error trigger for `ChangesFiltered`; `ChangesFiltered` 95.7% → 100%. (3) `TestChanges_ScanError` — same trigger for `Changes`; `Changes` 93.8% → 100%. (4) `TestScanRecords_OpenError` — `return fmt.Errorf("open audit file: …")` branch in `scanRecords` when `os.Open` fails with a non-`IsNotExist` error; triggered by embedding a null byte in the path (Windows `syscall.UTF16PtrFromString` returns `EINVAL`); `scanRecords` 89.5% → 94.7%. (5) `TestHandlePipeConn_ConnectionClosed` — `err != nil || n == 0` early-return path in `handlePipeConn`; exercised by closing the client connection before writing any data; `handlePipeConn` 91.4% → 97.1%. (6) `TestHandlePipeConn_StatusMarshalError` — `json.Marshal(result)` error path in the `"status"` case of `handlePipeConn`; triggered by a `math.NaN()` in `CheckResult.StateDurationSeconds` (same technique as `TestSave_MarshalError`); response is `{ok:false, error:"marshal result: …"}`. Root package 54.9% → 55.2%; pipe 21.7% → 23.0%. All tests pass, lint clean.

Previous state: **Seventy-sixth roam-mode pass complete.** Coverage sweep — 9 new tests across 3 files targeting previously-uncovered error branches: (1) `TestOpenAuditStore_MkdirAllError` — `os.MkdirAll` failure path in `OpenAuditStore` (regular file blocking the directory path); `OpenAuditStore` 75.0% → 100%. (2) `TestRecord_OpenFileError` — `os.OpenFile` failure path in `Record` (directory at audit file path prevents write-open); `Record` 80.0% → 90.0%. (3) `TestScanRecords_EmptyLineSkipped` — `len(line)==0 continue` branch in `scanRecords` was untested; test inserts a bare `\n` between valid JSONL records and asserts both records are returned; `scanRecords` 84.2% → 89.5%. (4) `TestHistory_ScanError` — `return nil, err` branch in `History` was unreachable; a >64 KiB line (exceeds `bufio.Scanner` buffer) triggers `bufio.ErrTooLong`; `History` 94.1% → 100%. (5) `TestPrune_ScanError` — `return 0, err` branch in `Prune` when `scanRecords` fails; same oversized-line trigger; `Prune` 70.6% → 76.5%. (6) `TestPrune_CreateTempError` — `os.Create(tmp)` failure path in `Prune` (directory blocks tmp path); covers the `create temp file` error return; `Prune` 76.5% (combined with above). (7) `TestClampRetention_LogsWarning_AboveMax` — `log != nil` branch for the above-max case in `ClampRetention` was untested (only the below-min case had a log test); `ClampRetention` 88.9% → 100%. (8) `TestSave_MkdirAllError` — `os.MkdirAll` failure in dashboard `save()` (regular file at parent dir path); `save` 75.0% → 100% (combined with next). (9) `TestSave_MarshalError` — `json.MarshalIndent` failure in dashboard `save()` triggered by injecting `math.NaN()` via `ServerInfo.LastResult.StateDurationSeconds`; both new store tests cover the remaining error branches. Root package 54.2% → 54.9%; dashboard `store.go:save` 75.0% → 100%. All tests pass, lint clean.

Previous state: **Seventy-fifth roam-mode pass complete.** Coverage sweep — 10 new tests across 4 files targeting previously-uncovered error branches: (1) `TestSendNotification_WebhookErrorIsLogged` / `_NtfyErrorIsLogged` — the `LogMsg(LvlWRN, ...)` error branches in `SendNotification` (lines 114/139) were unreachable in existing tests; both use a 500-returning httptest.Server to trigger the error path; `SendNotification` 96.4% → 100%. (2) `TestSendWebhook_DoFails_ReturnsError` / `TestSendNtfy_DoFails_ReturnsError` — the `httpClient.Do` failure path (connection refused) was distinct from the existing invalid-URL test (which fails at `http.NewRequest`); closing the test server before use triggers a connection-refused error; `sendWebhook` 94.7% → 100%, `sendNtfy` 93.8% → 100%. (3) `TestSendTestNotification_SkipsEmptyURLTarget` — the `continue` branch for empty-URL targets inside the dispatch loop was unreachable when only empty-URL targets were provided (caught earlier by `hasTargets`); test uses two targets (empty + real) to enter the loop and hit `continue`; `SendTestNotification` 93.1% → 96.6%. (4) `TestHandleDeleteServer_AuthenticatedUser_Returns200` — `auth != nil` branch logging `auth.Username` in `handleDeleteServer` was the last uncovered statement; pattern mirrors the same auth-injection tests added for other handlers; `handleDeleteServer` 93.3% → 100%. (5) `TestRateLimiter_TokensRefillClampedToCap` — `if b.tokens > rl.cap { b.tokens = rl.cap }` guard was never triggered; existing refill test added exactly 1 token (matching cap exactly); new test backdates by 10 s at rate=10/s, pushing refill 100× above cap; `Allow` 95.2% → 100%. (6) `TestSave_WriteFileError` / `TestSave_RenameError` — `save()` error branches (WriteFile and Rename) were unreachable; tests create a directory at the tmp or final path respectively to force I/O failure on Windows; `save` 56.2% → 75.0%. Root package 53.7% → 54.2%; dashboard 46.2% → 46.9%. All tests pass, lint clean.

Previous state: **Seventy-fourth roam-mode pass complete.** Coverage sweep — 4 new tests: (1) `TestHandleRegister_ReadBodyError_Returns400` — `io.ReadAll` failure branch in `handleRegister` was the sole uncovered statement; `handleRegister` 90.9% → 100%. (2) `TestHandleGetServer_EmptyHost_Returns400` — empty `PathValue("host")` guard in `handleGetServer` was untested; `handleGetServer` 83.3% → 100%. (3) `TestHandleNotifyTest_NilTestFuncUsesLoadConfigFunc_Success` / `_LoadError` — `testNotifyFunc==nil` production path was untestable because it called `dc.LoadConfig` directly; refactored `handleNotifyTest` to delegate to `testLoadConfigFunc` when set (mirrors the same pattern used in `handleGetNotifyConfig`); two tests cover the success path (testLoadConfigFunc returns config + live webhook) and the load-error path (testLoadConfigFunc returns error); `handleNotifyTest` 72.2% → 90.9%. Dashboard package 44.9% → 46.2%. All tests pass, lint clean.

Previous state: **Seventy-third roam-mode pass complete.** Feature + coverage sweep: (1) `GET /api/v1/servers/{host}` — new endpoint returns `ServerInfo` JSON for a single registered host (group-auth required); completes the REST design alongside `GET /api/v1/servers` (all) and `DELETE /api/v1/servers/{host}`; `ServerState.Get(hostname)` method added to `internal/dashboard/store.go`; 3 new tests (`TestHandleGetServer_ReturnsServerInfo`, `_UnknownHostReturns404`, `_NoLastResultReturnsRegisteredHost`). (2) Coverage sweep — 3 new tests: `TestHandleReport_ReadBodyError_Returns400` and `TestHandlePutNotifyConfig_ReadBodyError_Returns400` — `io.ReadAll` failure branch in both handlers was uncovered; `errReader` helper in `server_test.go` simulates a read error via a custom `io.Reader`; `handleReport` 88.9% → 100%; `handlePutNotifyConfig` 87.5% → 91.7%. `TestSendWebhook_MarshalError_ReturnsError` — `json.Marshal` error path in `sendWebhook` was unreachable with normal map values; `math.NaN()` triggers `UnsupportedValueError` and exercises the path; `sendWebhook` 89.5% → 94.7%. Dashboard package 43.2% → 44.9%. All tests pass, lint clean.

Previous state: **Seventy-second roam-mode pass complete.** Coverage sweep — 10 new tests across 3 files: (1) `TestSendNotification_PreviousModeInPayload` / `TestSendNotification_NoPreviousModeWhenNotTransition` — the webhook `previous_mode` field was untested; first test sets `result.Transition=true, TransitionFrom="AllowAll"` and asserts the field appears in the payload with the correct value; second test asserts the field is absent when `Transition=false`. (2) `TestSendWebhook_InvalidURL_ReturnsError` / `TestSendNtfy_InvalidURL_ReturnsError` — the `http.NewRequest` error path in both backends was uncovered; null-byte URL triggers the error. (3) `TestSendTestNotification_NtfyError_ReturnsError` — `SendTestNotification` ntfy error path: server returns 503, error must include the target URL. (4) `TestAuditStore_Close` — `AuditStore.Close()` was 0% covered despite being a no-op interface method; trivial test documents the nil-return contract. (5) `TestHandleRegister_AuthenticatedUser_Returns200` / `TestHandlePutNotifyConfig_AuthenticatedUser_Returns200` / `TestHandleNotifyTest_AuthenticatedUser_Returns200` — the `auth != nil` branch in all three handlers was untested; tests inject `AuthInfo` via `context.WithValue(r.Context(), authInfoKey, ...)` (same-package access). Root package 53.0% → 53.6%; dashboard 42.6% → 43.2%. All tests pass, lint clean.

Previous state: **Seventy-first roam-mode pass complete.** Coverage sweep + code quality: (1) `TestSendNotification_ResetsOnDrainOff` — `TriggerDrainOff` was tested for notification dispatch but not for its `clear(state.LastAlertNotify)` side-effect; new test mirrors `_ResetsOnHealthy` with `TriggerDrainOff` to confirm alert re-fires correctly after drain mode turns off. (2) `TestSendTestNotification_UnknownTypeSkipped` — target with unrecognised type and a non-empty URL was silently skipped but untested; new test documents this forward-compatibility contract. (3) `TestRateLimiter_StaleBucketPruned` — the lazy prune-on-access loop in `ipRateLimiter.Allow` was exercised only when buckets happened to expire during existing tests; new test artificially ages a bucket past `pruneAfter`, triggers pruning from a different IP, and asserts the stale entry is deleted. (4) `triggerList()` simplified — manual insertion sort replaced with `slices.Sort` (`"slices"` added to imports); the copy-then-sort pattern was unnecessary since `names` wasn't used after sorting. All tests pass, lint clean.

Previous state: **Seventieth roam-mode pass complete.** `drainctl notify set-webhook` and `set-ntfy` now accept optional `--secret`, `--triggers`, and `--repeat-minutes` flags (webhook-only for `--secret`). Previously the only field settable from CLI was the URL; configuring HMAC signing, trigger filtering, or repeat intervals required the dashboard UI or manual config.json edits — blocking scripted/MSI-driven deployments. Implementation: `notifyOverrides` struct with nil-pointer semantics (nil = preserve existing value); `parseTriggers` validates comma-separated names against `ValidTriggers`; `setNotifyTarget` applies overrides after `upsertNotifyTarget` in a single save; `triggerList()` helper produces sorted, deterministic output for help text and errors. `docs/guide.html` §5 (CLI Notifications) and §7 (Notifications) updated with examples. All tests pass, lint clean.

Previous state: Sixty-ninth pass — Feature + coverage sweep: (1) `CheckResult.Write` CSV now includes 4 session columns (`active_sessions`, `disconnected_sessions`, `total_sessions`, `max_sessions`) — history CSV already had them (Roam #64) but the check CSV omitted them; nil `Sessions` produces empty fields (backward-compatible); 3 new tests. (2) `handleHealth` coverage to 100% — `case "Grace"` and `default` branches were untested; 2 new tests (`TestHandleHealth_GraceServerCounted`, `TestHandleHealth_ErrorStatusCountedAsUnknown`). (3) `formatSessionSummaryLine` to 100% — nil guard and plain-format nil-summary path; 2 new tests. (4) `Config.Validate` AuditPath branch covered — 2 new tests (`TestValidate_SetsDefaultAuditPath`, `TestValidate_PreservesExistingAuditPath`). All tests pass, lint clean.

Previous state: Sixty-eighth pass — Test coverage sweep: (1) `ChangesFiltered` with `n>0` — the ring buffer/limit path was untested (69.6% coverage); added three sub-tests: `limit_applied_returns_newest_n_transitions` (n=1), `limit_with_time_filter` (n=2 + since bound), `ring_buffer_evicts_oldest_when_over_limit` (3 transitions, limit=2); coverage 69.6% → 95.7%. (2) `CheckResult.Write` nil-pointer paths — `formatTimePtr` and `formatFloatPtr` nil branches were unreachable by existing tests (always non-nil `StateSince`/`StateDurationSeconds`); added `TestCheckResultWrite_CSV_NilPointers` (nil both pointers → empty CSV columns, no `<nil>` string) and `TestCheckResultWrite_Table_NilDuration` (nil duration → `"n/a"` in STATE DURATION column); `formatTimePtr` / `formatFloatPtr` / `formatAge` all reached 100%. (3) `audit.go` CRLF normalisation via `gofmt -w .` — Windows git checkout had re-introduced CRLF drift since Roam #22, breaking `just lint`.

Previous state: Sixty-seventh pass — Three `format.go` quality fixes: (1) bug — `WriteSessions` plain format silently dropped the `station` field when a `UserName` was present (the convoluted `fields[1:]` + overwrite pattern prepended `user=X` and discarded station); fixed to `append([]string{user=X}, fields...)` so all four fields (`user`, `session_id`, `station`, `state`) are always present. (2) UX — `WriteHistoryRecords` table format showed raw RFC3339 timestamps (`2026-04-01T10:01:00+00:00`) while `WriteHistory` table used `2006-01-02 15:04:05`; `drainctl history --format table` now looks identical whether the service is running (pipe path) or not (direct path). (3) code quality — `joinFields` helper removed and replaced with `strings.Join(fields, " ")` throughout; test suite updated (removed `TestJoinFields_*`, added station and session_id assertions to `TestWriteSessions_Plain_LogLines`, added RFC3339 rejection assertion to `TestWriteHistoryRecords_Table`).

Previous state: Sixty-sixth pass — ntfy notification titles made human-readable — `ntfyTitle(trigger, host)` helper replaces the raw trigger-name format (`"DrainCtl: drain_on on HOSTNAME"`) with readable labels (`"DrainCtl: Drain Mode Active on HOSTNAME"`, `"Connections Restored"`, `"Grace Period Active"`, `"Alert: Drain Exceeded Grace Period"`, `"All Connections Allowed"`, `"Session Utilization Warning"`). Unknown triggers fall back to the raw name. `docs/guide.html` updated: `drainctl configure show` added to Section 5 (CLI Usage) and Section 3 (Configuration) — the command was added in Roam #65 but never documented. 14 new tests (`TestNtfyTitle` 7 sub-tests, `TestSendNotification_NtfyTitleIsReadable` 5 sub-tests, plus `TestSendNotification_NtfySessionWarningMessage` already passed).

## Completed Work

| Phase | Features | Tags |
|-------|----------|------|
| Roam #1 | Webhook HMAC-SHA256 signing (`Secret` field on `NotificationTarget`; `X-DrainCtl-Signature` header) | security, notify |
| Roam #1 | `GET /api/v1/health` dashboard endpoint (unauthenticated; version + server counts) | feature, dashboard |
| Roam #1 | Hostname validation in `handleRegister` (trim whitespace, DNS 253-char limit) | security, validation |
| Roam #1 | URL scheme validation in `Config.Validate` (strip non-http/https notification targets) | security, validation |
| Roam #1 | First unit test suite — 27 tests across `config_test.go` and `notify_test.go` | testing |
| Roam #1 | gofmt pre-existing violations fixed across all Go source files | code quality |
| Roam #2 | `internal/dashboard` handler tests — 19 tests for `handleHealth`, `handleRegister`, `handleReport` | testing, dashboard |
| Roam #2 | `audit.go` streaming refactor — `readAll` replaced with `scanRecords(fn)` + rolling-window `History`/`Changes` | performance, code quality |
| Roam #3 | `GET /api/v1/history/{host}` — per-host CheckResult ring buffer (cap 100) in `ServerState`; newest-first JSON; `?limit` param; 7 tests | feature, dashboard, testing |
| Roam #4 | Exponential backoff for dashboard config fetch — `backoffTicks(failures)` doubles interval per failure (10→20→40→80→160→320 polls, cap); resets on success or URL change; 1 test | resilience, svc |
| Roam #4 | `drainctl configure` interactive wizard — prompts each setting with current value as default; flag-driven path unchanged for MSI installer; command unhidden | feature, UX, CLI |
| Roam #5 | SSPI `RequireGroup` — `RevertToSelf` called after `isGroupMember` (all paths); `procRevertToSelf` via advapi32.dll | security, dashboard |
| Roam #5 | Config hot-reload: `pruneNotifyState` removes stale `LastAlertNotify` entries for URLs removed from targets | correctness, svc |
| Roam #5 | `drainctl history --since`/`--until` — RFC3339 time-bounded queries; `HistoryFiltered`/`ChangesFiltered` on `AuditStore`; post-filter on pipe path; 11 tests | feature, CLI, testing |
| Roam #6 | `drainctl service start/stop/status` — replaced instruction-printing stubs with real SCM calls via `golang.org/x/sys/windows/svc/mgr`; 30 s timeout polling with 500 ms sleep; `svcStateString` helper; start/stop require admin, status does not | feature, CLI |
| Roam #7 | `internal/svc/handler.go` split into `handler.go` (service lifecycle: `drainService`, `Execute`, `serviceHandler`, `RunService`, `backoffTicks`, event constants) and `check.go` (check cycle: `svcRunCheck`, `pruneNotifyState`, `applyRemoteConfig`); 623 → 411 + 227 lines | code quality |
| Roam #7 | 20 tests for `internal/store/memstore.go` — covers `LastObservation`, `History` (ordering, limits), `Changes` (filtering, limits), `StateSince` (empty, all-same-mode, after-transition, absent-mode, returns-to-current), `Flush`/`Close` persistence, `Prune`, `FlushIfDirty`, concurrent access | testing |
| Roam #8 | 22 tests for `audit.go` (`AuditStore`) — `LastObservation` (empty, single, newest-of-many, nonexistent file), `History` (empty, newest-first, limit, limit=0, limit>total), `Changes` (empty store, only transitions, limited to N), `StateSince` (empty, all-same, after transition, absent mode, returns-to-current), `Prune` (removes old, keeps recent, empty store, prunes all), `scanRecords` malformed-line skip | testing |
| Roam #9 | `handleHealth` correctness: `Grace` status was miscounted as `healthy`; added separate `grace` field to JSON response (backward-compatible) | correctness, dashboard |
| Roam #9 | `securityMiddleware`: `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy` headers added to all responses; HSTS remains TLS-only, chained on top | security, dashboard |
| Roam #9 | `handleDeleteServer`: removed unreachable URL-path fallback (predated Go 1.22 `ServeMux` `PathValue`); 11 new tests for `handleServers`, `handleDeleteServer`, and Grace health counting | code quality, testing |
| Roam #10 | 27 tests for `internal/dashboard/store.go` (`ServerState`) — `NewServerState` (empty dir, load existing, corrupt file), `Register` (idempotent, sets `RegisteredAt`), `Remove` (bool return), `IsRegistered`, `Update` (`LastResult`/`LastSeen`, unregistered no-op), `HostHistory` (newest-first, n-limit, n=0 returns all, ring cap at `historyMax`, ring evicts oldest), `All` (empty slice, hostname sort, snapshot isolation), persistence (register/remove/`LastResult` survive reload, tmp→rename pattern, valid JSON), concurrent access under `-race` | testing |
| Roam #11 | `POST /api/v1/notify-test` — sends a test notification to all configured targets; returns 400 with error message when no targets or webhook fails; `testNotifyFunc` hook on `DashboardServer` for test isolation; 6 handler tests (no-targets→400, success→200, network error, config error, mock webhook call, HMAC signature header) | feature, dashboard, testing |
| Roam #11 | Dashboard UI: webhook HMAC secret field — password input per notification target row (webhook-only, hidden for ntfy); populated from `loadNotifyConfig`, included in `saveNotifyConfig` payload; shows placeholder "Optional signing secret" | feature, UX, dashboard |
| Roam #11 | Dashboard UI: "Send Test" button in settings modal — calls `POST /api/v1/notify-test`, shows "Sending…" while in-flight, displays success/error in `cfg-status`, re-enables on completion | feature, UX, dashboard |
| Roam #12 | `testLoadConfigFunc` + `testPutNotifyConfigFunc` injection hooks on `DashboardServer` — same pattern as `testNotifyFunc`; makes `handleGetNotifyConfig` and `handlePutNotifyConfig` fully unit-testable without ProgramData; 10 handler tests (GET: 200+fields, empty array, 500 on load error, multiple targets; PUT: 200, 400 bad JSON, 500 update error, arg capture, partial update, webhook secret roundtrip) | testing, dashboard |
| Roam #12 | `Content-Security-Policy` header in `securityMiddleware` — `default-src 'none'`; `script-src 'unsafe-inline'`; `style-src 'unsafe-inline' https://fonts.googleapis.com`; `font-src https://fonts.gstatic.com`; `img-src 'self' data:`; `connect-src 'self'`; `frame-ancestors 'self'`; `base-uri 'self'`; `form-action 'self'`; 3 middleware tests (security headers presence, HSTS absent in plain middleware, HSTS present in hsts wrapper) | security, dashboard |

| Roam #13 | Dashboard UI: per-host history drill-down modal — "History" button on each server card calls `GET /api/v1/history/{host}?limit=20` (existing endpoint); modal shows timestamp, status badge, mode, duration, changed_by, session counts per entry; ESC closes | feature, UX, dashboard |
| Roam #13 | Dashboard UI: Grace remaining time — server cards in Grace state show "Grace Left: Xm Ys left" (amber) computed from `graceMinutes` (cached from `loadNotifyConfig`) and `state_duration_seconds`; `graceMinutes` initialised to `null` so row is hidden until config loads | feature, UX, dashboard |
| Roam #13 | Dashboard UI: last-refresh timestamp in footer — `refresh()` writes "Updated HH:MM:SS" to `#footer-updated` on each successful poll, replacing the static "Auto-refresh 30s" text | UX, dashboard |
| Roam #14 | Dashboard UI: server filter bar — text search input + status pills (All/Healthy/Grace/Alert/Offline) above server grid; `filterGrid()` shows/hides `.srv` cards client-side; "No servers match" notice when result is empty | feature, UX, dashboard |
| Roam #14 | Dashboard UI: safe button delegation — removed `onclick="showHistory('...')"` / `onclick="rm('...')"` inline handlers; server cards carry `data-host` / `data-status` attributes; single delegated listener on `#grid` dispatches History and Remove actions | security, code quality, dashboard |
| Roam #15 | Dashboard accessibility — `role="img"` + dynamic `aria-label` on state bar; `role="dialog"` + `aria-modal` + `aria-labelledby` on settings and history overlays; `aria-label` on close/expand/gear buttons; `role="log"` + `aria-live="polite"` on event log; `aria-label="View history for {host}"` / `aria-label="Remove {host}"` on server card buttons; focus returns to gear button on settings close | a11y, dashboard |
| Roam #15 | Landing page mobile nav — hamburger toggle button (`.nav-toggle`) appears at ≤ 640px; click opens full-width dropdown (`nav-links.open`); `aria-expanded` + `aria-controls` wired; links auto-close the menu on click | UX, a11y, docs |
| Roam #16 | Bug fix: `NotifyState.LastSessionWarnNotify` — `session_warning` repeat-tracking was sharing `LastAlertNotify` (URL-keyed) with `TriggerAlert`; on `TriggerHealthy`/`TriggerDrainOff` the cleanup compared URLs against the literal string `"session_warning"` (always false), deleting all entries. Fixed by splitting into two maps: `LastAlertNotify` (cleared on healthy) and `LastSessionWarnNotify` (persists); `pruneNotifyState` updated to prune both | correctness, svc, notify |
| Roam #16 | Dashboard settings: client-side validation in `readTargets()` — webhook URLs must pass `new URL()` parse with `http:` or `https:` protocol; invalid URLs get red border and block save with descriptive message | security, UX, dashboard |
| Roam #16 | Dashboard settings: `saveNotifyConfig()` now validates session threshold (0–100) and grace period (1–1440) before submission — fixes silent bug where `parseInt("0") \|\| 80` silently overrode "Disable session warnings"; out-of-range inputs get red border + inline error | correctness, UX, dashboard |
| Roam #16 | 5 new tests: `TestSendNotification_SessionWarningPreservedThroughHealthy`, `TestSendNotification_IndependentTargetState` (notify_test.go); `TestValidate_EmptyTriggersDefaulted`, `TestValidate_StripsUnknownTriggers`, `TestValidate_PreservesValidTriggers` (config_test.go) | testing |
| Roam #17 | `classifyState(drainActive bool, stateDur, gracePeriod time.Duration) (status, message string, exitCode int)` — extracted from `svc/check.go` and `svc/handler.go`; replaces 3 duplicate if/else blocks | code quality, svc |
| Roam #17 | `setNotifyTarget(fileCfg *dc.Config, typ, url string, log dc.LogFunc) error` — extracted from `cmd/drainctl/main.go`; `set-webhook` and `set-ntfy` RunE bodies reduced to 5 lines each | code quality, CLI |
| Roam #17 | Dashboard settings modal UX: "Saved successfully" auto-dismisses after 4 s; save button disabled during fetch; `loadNotifyConfig()` called on modal open (always fresh data); loading placeholder shown while fetch is in-flight | UX, dashboard |
| Roam #17 | PS module: 3 unused `$result` and `\| Out-Null` occurrences replaced with `$null =` in `Install-RDSHDrainAudit`, `Enable-RDSHDrainDashboard`, `Disable-RDSHDrainDashboard` | code quality, ps |
| Roam #17 | Lint: removed unused `ptr()` helper from `audit_filter_test.go`; dropped redundant `http.Handler` type annotation in `server.go`; added `json.Decode` error checks in `server_test.go` (4 pre-existing golangci-lint warnings cleared; lint now reports 0 issues) | code quality, testing |
| Roam #18 | `Config.Validate`: `Dashboard.Port` guard widened from `== 0` to `< 1 \|\| > 65535` — negative or out-of-range values in config.json were silently accepted and caused bind failures; `Dashboard.Group` trimmed before empty check — all-whitespace strings passed the guard but broke SSPI group matching silently; 4 new tests (`TestValidate_DashboardPortClamped`, `TestValidate_DashboardPortPreservesValid`, `TestValidate_DashboardGroupTrimsWhitespace`, `TestValidate_DashboardGroupPreservesNormal`) | correctness, config, testing |
| Roam #19 | Dashboard UI: dark/light mode manual toggle — moon/sun button in nav; `html[data-theme]` attribute overrides the OS `prefers-color-scheme` media query; preference persisted in `localStorage`; FOUC-prevention inline script in `<head>`; chart rebuilt on toggle to pick up recomputed CSS vars | UX, dashboard |
| Roam #19 | `QueryRegistryChangeUser`: replaced bare `exec.Command` with `exec.CommandContext` and a 15 s timeout — previously a large or slow Security event log could block the service check cycle indefinitely without any deadline | correctness, svc |
| Roam #19 | `ipRateLimiter` (token bucket, per-IP) added to `internal/dashboard/ratelimit.go`; `rateLimitMiddleware` wraps all routes (10 req/s sustained, burst 60) — prevents runaway clients from hammering the unauthenticated `/api/v1/health` endpoint or triggering excessive SSPI negotiations; lazy pruning of stale buckets after 5 min; 7 new tests (`TestRateLimiter_AllowsWithinBurst`, `_RejectsAfterBurst`, `_IndependentIPs`, `_TokensRefill`, `_Middleware_Allows`, `_Middleware_Rejects`, `_Middleware_BadRemoteAddr`) | security, dashboard, testing |
| Roam #20 | `marshalJSON` error path in `cmd/cshared/exports.go` delegates to `marshalError` — eliminates potential JSON injection from error message characters (`"`, `\`) via string concatenation | security, DLL |
| Roam #20 | `NotifyState.LastSessionWarnNotify` eagerly initialised alongside `LastAlertNotify` at service startup in `internal/svc/handler.go` (was lazily initialised on first use) | code quality, svc |
| Roam #20 | Dashboard `.srv-host`: `overflow:hidden; text-overflow:ellipsis; white-space:nowrap; min-width:0` — long hostnames now truncate gracefully in the flexbox card header instead of overflowing | UX, dashboard |
| Roam #20 | Dashboard history modal: `showHistory(hostname, trigger)` saves the triggering button; `closeHistory()` restores focus on close — fixes keyboard navigation gap where focus fell to `<body>` | a11y, dashboard |
| Roam #20 | Dashboard filter pills: `role="button"`, `tabindex="0"`, `aria-pressed` attribute, keyboard activation (Enter/Space), `role="group" aria-label` on container; `activateFilterPill()` helper keeps active state and `aria-pressed` in sync | a11y, dashboard |
| Roam #21 | `handleHealth` staleness: servers with `last_seen` older than 10 minutes are now counted as `offline` (new field in JSON response) instead of retaining their last-reported status — eliminates false-healthy/alerting counts for offline servers; explicit `case "Healthy"` replaces `default` fallback; 3 new tests | correctness, dashboard, testing |
| Roam #21 | Dashboard filter/search state persistence: `activeFilter` and search text saved to `localStorage` (`drainctl-filter`, `drainctl-search`) on every change; restored on page load so filter state survives refresh | UX, dashboard |
| Roam #21 | Dashboard OS dark mode live update: `prefers-color-scheme` change listener calls `updateThemeBtn()` and re-renders chart when no manual theme override is set — theme and chart colours now track OS preference changes at runtime | UX, dashboard |
| Roam #22 | `handleRegister` RFC 1123 hostname validation: `hostnameRE` regex rejects labels with invalid chars (underscores, slashes, null bytes, etc.), leading/trailing hyphens, empty labels, and labels exceeding 63 chars; `TestHandleRegister_MaxLengthHostname` updated to use valid multi-label hostname; `TestHandleRegister_InvalidCharacters` (10 sub-tests) + `TestHandleRegister_ValidHostnameFormats` (6 sub-tests) added | security, validation, testing |
| Roam #22 | `applyRemoteConfig` input clamping: `SessionWarningThreshold` capped at 100 (was uncapped), `GracePeriod` capped at 1440 minutes — prevents a misconfigured or compromised dashboard from injecting out-of-range values into the service; 7 new tests in `check_test.go` | correctness, svc, testing |
| Roam #22 | `check_test.go` added: 5 tests for `classifyState` covering Healthy, Grace within period, Alert exceeds period, boundary (Grace at ==gracePeriod), and just-over-boundary (Alert) | testing, svc |
| Roam #22 | Dashboard UX: `rm()` two-stage inline confirm — first click changes button to "Sure?" (solid red fill, `data-confirm="1"`, updated `aria-label`), second click executes DELETE; auto-cancels after 5 s; errors shown via `showGridToast()` (dismisses after 5 s, `role="alert"`) instead of native `alert()` | UX, a11y, dashboard |
| Roam #22 | gofmt CRLF normalization: `gofmt -w .` applied to entire tree to eliminate CRLF/LF drift on Windows CI | code quality |
| Roam #23 | `serviceHandler.cfg` race fix: `atomic.Pointer[dc.ServiceConfig]` replaces bare pointer field — Execute loop hot-reload write and pipe server goroutine HandleStatus read were unsynchronised | correctness, svc |
| Roam #23 | `ServerState.Remove()` history cleanup: `delete(s.history, hostname)` added alongside `delete(s.servers, hostname)` — ring buffer was leaked for re-registered-then-removed hosts; 1 new test | correctness, dashboard, testing |
| Roam #23 | `format_test.go`: 19 new tests — `ParseFormat` (5 valid + 1 invalid), `ComputeStateDurations` (empty/single/same-mode/transition-reset), `AuditToHistory` (field mapping, `KeyModified` omit/include), `FormatBool` (nil/true/false), `or`, `joinFields` | testing |
| Roam #24 | `loadNotifyConfig` JS bug: `c.session_warning_threshold \|\| 80` silently overrode 0 (disabled) with 80 — fixed to `!= null` null-coalescing guard; Roam #16 fixed the save path but the load path was missed | correctness, dashboard |
| Roam #24 | `saveNotifyConfig` JS: reads response body on error (`await r.text()`) so the server's message is displayed instead of a bare "Error: HTTP N" | UX, dashboard |
| Roam #24 | `pruneNotifyState` tests: 6 new tests in `check_test.go` — stale alert/session-warn removal, active-entry preservation, empty-targets full-clear, blank-URL target skipping | testing, svc |
| Roam #24 | Dashboard server cards and history modal: `title` attribute added to hostname span with full FQDN — hover reveals the complete hostname when only the short label is displayed | UX, dashboard |
| Roam #25 | `handlePutNotifyConfig`: replaced three separate LoadConfig+save cycles with single `UpdateNotifySettings()` call; changed `in.Notifications` from `[]NotificationTarget` to `*[]NotificationTarget` so an absent field is a no-op (was silently clearing all targets on partial updates); 1 new test `TestHandlePutNotifyConfig_AbsentNotifications_PassedAsNil`; updated 5 existing test hook signatures | correctness, dashboard, testing |
| Roam #25 | `MemAuditStore.Prune`: `json.Marshal` error now skips the record instead of writing an empty line — matches `flushLocked` behaviour | correctness, svc |
| Roam #25 | `ipRateLimiter.Allow`: removed redundant `addr != ip` guard in prune loop; current IP's `lastSeen` is always `now` after refill so it is never pruned regardless | code quality, dashboard |
| Roam #26 | `ipRateLimiter.Allow` returns `(bool, time.Duration)` — `rateLimitMiddleware` now sets `Retry-After: N` (whole seconds, RFC 7231 SHOULD) on 429 responses so well-behaved clients back off correctly; 1 new test `TestRateLimiter_Middleware_Rejects_SetsRetryAfterHeader`; 4 existing tests updated for new signature | correctness, security, dashboard, testing |
| Roam #26 | `handlePutNotifyConfig`: validates `session_warning_threshold` (0–100) and `grace_period` (1–1440) before calling `UpdateNotifySettings` — out-of-range values now return 400 Bad Request instead of 500 Internal Server Error; 4 new tests | correctness, dashboard, testing |
| Roam #27 | Wall-clock config re-fetch timer: `backoffTicks(int) int` → `backoffDuration(int) time.Duration` (5 min base, doubles per failure, 160 min cap); `pollsSinceConfigFetch` counter → `lastConfigFetch time.Time` — re-fetch interval now independent of `PollInterval` runtime changes; `backoff_test.go` updated; log message changed from `next_retry_polls=N` to `next_retry_in=Xm` | correctness, svc |
| Roam #27 | uPlot chart `setData()` optimisation: `buildChartData()` extracted; `renderChart()` compares container width + CSS colour fingerprint against last-creation values — data-only 30 s updates call `chartInstance.setData()` instead of destroy+recreate (no per-poll flicker); stale `now` closure in x-axis formatter fixed to recompute `Date.now()/1000` on each axis draw | perf, UX, dashboard |
| Roam #28 | `AuditStore.Prune`: `data, _ := json.Marshal(r)` → `data, err := json.Marshal(r); if err != nil { continue }` — silent write of empty line on marshal error is now a skip, matching `MemAuditStore.Prune` behaviour; the corrupt empty line would have been silently skipped on re-read but the record would be lost without indication | correctness, audit |
| Roam #28 | `sendWebhook` / `sendNtfy`: add `_, _ = io.Copy(io.Discard, resp.Body)` before return — drains the response body so the HTTP transport reuses the keep-alive connection; without the drain, each outgoing notification opened a new TCP connection | perf, notify |
| Roam #28 | `sendNtfy`: 4 new direct tests (`TestSendNtfy_Success`, `TestSendNtfy_NonSuccessStatus`, `TestSendNtfy_SetsHeaders`, `TestSendNotification_CallsNtfy`) — `sendNtfy` previously had zero direct test coverage despite being the second notification backend | testing, notify |
| Roam #29 | `pipeConn.SetDeadline` forwarding fix: the stored `c.deadline` field was never applied to the underlying `os.File`; all `conn.SetDeadline()` calls in `handlePipeConn` and `pipeRPC` were silently no-ops; now delegates to `c.file.SetDeadline(t)` so overlapped server-side handles enforce the 5 s deadline | correctness, svc |
| Roam #29 | Dashboard `renderHistoryModal` XSS: `ts = e.time` fallback (NaN date) was injected unescaped into `innerHTML`; fixed to `esc(e.time)` — server timestamps are always valid RFC 3339 in practice, but defensive escaping is required | security, dashboard |
| Roam #29 | `internal/pipe/pipe_test.go`: 7 new tests for `handlePipeConn` via `net.Pipe()` — status OK, status nil result → error, history records, default limit (0 → 50), `ChangesOnly` flag, unknown command → error, invalid JSON → error; first test coverage for the named pipe IPC layer | testing, svc |
| Roam #30 | `SendNotification` webhook payload now includes `"sessions"` field (`active_sessions`, `disconnected_sessions`, `total_sessions`, `max_sessions`, `utilization_pct`) when `result.Sessions != nil` — previously all notification triggers, including `session_warning`, sent no session data to webhook consumers | feature, notify |
| Roam #30 | ntfy `session_warning` body overridden to "Session utilization at N% (X/Y sessions)." — previously sent `result.Message` which for a Healthy server reads "All connections allowed." and contains no session information; tags changed to `busts_in_silhouette` | UX, notify |
| Roam #30 | Dashboard `render()` prunes stale `prev` map entries after each refresh — servers removed from the dashboard left behind entries that could surface as false "transition" events if the server was later re-added; 3 new tests (`TestSendNotification_WebhookPayloadIncludesSessions`, `_OmitsSessionsWhenNil`, `_NtfySessionWarningMessage`) | correctness, dashboard, testing |
| Roam #31 | Dashboard `renderHistoryModal` bug fix: `e.time` → `e.timestamp` — `CheckResult` serializes its timestamp as `"timestamp"` in JSON, so `e.time` was always `undefined`; all history modal entries showed a blank timestamp | correctness, dashboard |
| Roam #31 | Dashboard `stateHistory` bug fix: `push` is now conditional on `t > 0` — polls while no servers are registered produced `{ t: 0 }` entries; `renderChart` exits early when `stateHistory[0].t === 0`, so the chart would not appear for up to 30 minutes (MAX_HIST × 30 s) after the first servers registered on a fresh install | correctness, dashboard |
| Roam #32 | `Config.Validate`: unknown `NotificationTarget.Type` values now stripped with a warning log — a target whose `Type` was neither `"webhook"` nor `"ntfy"` was previously saved to config and silently never fired notifications; 3 new tests (`TestValidate_StripsUnknownType`, `_UnknownTypeLogsWarning`, `_PreservesValidTypes`) | correctness, config, testing |
| Roam #32 | `internal/dashboard/client.go` body drain: `negotiateRequest` 401 branch, `ReportState` success path, and `RemoveServer` success path now drain response bodies via `io.Copy(io.Discard, ...)` before `Close()` — Go's `http.Transport` requires the body to be fully read before connection reuse; `notify.go` already did this but the dashboard client did not | correctness, performance |
| Roam #34 | `ClassifyState(drainActive bool, stateDur, gracePeriod time.Duration) (status, message string, exitCode int)` promoted from `internal/svc` private helper to exported root-package function — eliminates last duplicate state classification; `svc/check.go` and `svc/handler.go` now call `dc.ClassifyState()`; root `Check()` refactored to use it with `effectiveDur` (audit-store duration when available, else registry KeyModified delta); `classify_test.go` adds 7 tests in root package; `TestClassifyState_*` removed from `svc/check_test.go` | code quality, testing |
| Roam #34 | Dashboard history modal: transition records now visually distinguished — accent-coloured left border (`hist-entry-transition` CSS class) + filled dot indicator (`hist-transition-dot`) inserted before the status badge; detail line shows `from → to` mode labels for transition entries instead of bare `mode=...`; `transition` and `transition_from` fields already present in `CheckResult` JSON, no API changes needed | UX, dashboard |
| Roam #33 | `PipeHandler.HandleStatus` interface simplified: removed dead `gracePeriod time.Duration` parameter — always passed as `0` via the pipe, implementation always reads from `serviceHandler.cfg` store; `serviceHandler.HandleStatus`, `pipe_test.go` mock and capture handlers updated accordingly | code quality, svc |
| Roam #33 | `GET /api/v1/history/{host}` gains `?changes_only=1` (or `true`) query parameter — fetches the full ring buffer, filters to `Transition=true` records, then applies `limit`; default limit lowered from 50 → 20 to match the UI's fetch; 4 new tests (`TestHandleHistory_ChangesOnly_ReturnsOnlyTransitions`, `_EmptyWhenNoTransitions`, `_RespectsLimit`, `TestHandleHistory_DefaultLimitIs20`) | feature, dashboard, testing |
| Roam #33 | Dashboard history modal: "Transitions only" toggle button added next to the close button — when active, re-fetches with `?changes_only=1&limit=20`; resets to inactive (all entries) each time the modal is opened for a new host | feature, UX, dashboard |

| Roam #35 | `registerWithDashboard` helper extracted from `Execute` loop: registration + auto-pin logic moved to a standalone function; `dashRegistered bool` flag added to the Execute loop — when startup registration fails (dashboard temporarily unavailable), every subsequent poll tick retries until it succeeds; on config-reload URL change `dashRegistered` is reset to `false` so re-registration against the new URL is triggered automatically; fixes silent 403 loop where `ReportState` was rejected indefinitely after a failed startup registration | correctness, svc |
| Roam #35 | `handleHistory`: for `changesOnly=false` queries, passes `limit` directly to `HostHistory()` instead of always fetching `historyMax=100` records — avoids allocating the full ring when fewer records are requested (e.g. the default limit=20 path now fetches 20 records, not 100); `changesOnly=true` still fetches the full ring so filtering has the complete picture | perf, dashboard |
| Roam #35 | `Config.Validate`: `GracePeriod` guard widened from `< 1` to `< 1 \|\| > 1440` — values above 1440 minutes in a hand-edited config.json were silently accepted and unclamped (only `applyRemoteConfig` clamped them for dashboard-sourced config); now consistent with `UpdateGracePeriod` and `UpdateNotifySettings` validation; 2 new tests (`TestValidate_GracePeriodClamped`, `TestValidate_GracePeriodPreservesValid`) | correctness, config, testing |

| Roam #36 | `handler.cfg.Store(&cfg)` added after both `applyRemoteConfig` call sites (startup dashboard config fetch + poll-tick refresh) — without the atomic store, the pipe handler's `HandleStatus` never acquired the happens-before edge for dashboard-sourced `GracePeriod`/`SessionWarningThreshold` mutations; `drainctl status` showed the stale grace period from config.json instead of the dashboard-set value | correctness, svc |
| Roam #36 | `handleGetNotifyConfig`: nil `cfg.Notifications` (old config file missing the "notifications" JSON field) normalised to `[]dc.NotificationTarget{}` before serialisation — prevented `"notifications": null` in the API response; 1 new test `TestHandleGetNotifyConfig_NilNotificationsReturnsEmptyArray` | correctness, dashboard, testing |
| Roam #36 | Dashboard history modal: `sess=N/0` display suppressed when `max_sessions==0` (unlimited, i.e. `MaxInstanceCount` not configured in Terminal Server registry) — now shows `sess=N` only when max is not configured, `sess=N/M` when a cap is set | correctness, UX, dashboard |

| Roam #37 | `loadNotifyConfig()` error handling: `if (!r.ok) return` path and the catch block both now write an inline error message to the target-list div instead of leaving "Loading…" visible indefinitely — users can see the failure and close/reopen settings to retry | correctness, UX, dashboard |
| Roam #37 | `addTargetRow()` type-switch: when switching from ntfy → webhook, the URL field is cleared if the value is not already an `http(s)://` URL — a bare ntfy topic name is invalid as a webhook URL and would produce a confusing red-border validation error at save time with no clear explanation | correctness, UX, dashboard |

| Roam #38 | Dashboard event log `sessInfo`: `max_sessions > 0` guard added — when `MaxInstanceCount` is not configured (unlimited), the log was showing `"N/0 total (0%)"` instead of `"N total"`; now consistent with the server card gauge and history modal which both already guarded this | correctness, UX, dashboard |
| Roam #38 | Dashboard event log transition message: changed from `"status=Healthy → from Alert"` to `"Alert → Healthy"` — the previous format combined a redundant `"status="` prefix with an arrow that pointed to the new status, then `"from"` pointing back, which was confusing to read; the new format matches the left-to-right "prev → next" convention used in the history modal and service event log | correctness, UX, dashboard |
| Roam #38 | `securityMiddleware`: `Cache-Control: no-store` added as the default for all responses — authenticated API endpoints (`/api/v1/servers`, `/api/v1/history`, `/api/v1/notify-config`, etc.) return sensitive health data that must not be stored in browser history or intermediate caches; `handleUI` continues to override with `no-cache` and the favicon handler overrides with `public, max-age=86400`; 1 test updated (`TestSecurityMiddleware_SetsExpectedHeaders`) | security, dashboard, testing |

| Roam #39 | `resetSessionWarnCooldown`: when utilization is below `SessionWarningThreshold` (or max sessions is 0/unknown), all `LastSessionWarnNotify` entries are cleared — previously the per-target cooldown persisted indefinitely so `RepeatMinutes=0` targets never fired again after a single warning even when sessions recovered; `resetSessionWarnCooldown` extracted as a testable helper with 3 new tests | correctness, svc, notify, testing |
| Roam #39 | Dashboard history modal: `loadHistory` uses `AbortController` to cancel any in-flight request before starting a new one — rapid toggling of "Transitions only" (or quickly opening history for a different host) could let a stale slow response overwrite the result of a newer fast one; `closeHistory` also aborts any pending request on close | correctness, UX, dashboard |
| Roam #39 | Dashboard remove-confirm: `clearTimeout(_rmTimers[h])` added before setting the new timer on the first confirm-click — without the clear, if the grid re-rendered during the 5-second window the old timer's `delete _rmTimers[h]` would remove the new timer's reference, making it uncancel­able | correctness, dashboard |
| Roam #39 | Dashboard settings target remove button: `aria-label="Remove notification target"` added — the button only had a `title` attribute which screen readers do not announce consistently | a11y, dashboard |

| Roam #40 | Dashboard `loadNotifyConfig`: `AbortController` added — closing and reopening settings cancels any in-flight fetch so a slow response cannot overwrite the freshly-loaded form; `toggleSettings` close path calls `_cfgAbortCtrl.abort()` matching the `closeHistory` pattern | correctness, UX, dashboard |
| Roam #40 | Dashboard `saveNotifyConfig` dismiss timer: `clearTimeout(_saveStatusTimer)` before setting the new 4 s auto-dismiss — without the cancel, rapid saves could leave a stale timeout that cleared a subsequently-shown error message | correctness, dashboard |
| Roam #40 | Dashboard remove-confirm auto-cancel: setTimeout callback now re-queries the button via `document.querySelector('.btn-rm[data-host="..."]')` instead of using the captured `rmBtn` variable — the 30 s grid refresh replaces all DOM nodes, so the captured reference points to a detached element by the time the 5 s timeout fires | correctness, dashboard |
| Roam #40 | Dashboard empty-state hint: `location.host` wrapped in `esc()` — hostname never contains HTML-special characters, but all browser/server-provided values injected into `innerHTML` must be escaped for defence-in-depth | security, dashboard |
| Roam #40 | 5 new `SendTestNotification` tests — `_NoTargets_ReturnsError`, `_EmptyURLTargets_ReturnsError`, `_WebhookSuccess` (payload `event=test` verified), `_WebhookError_ReturnsError`, `_NtfySuccess`, `_MultipleTargets_CallsAll`; `SendTestNotification` previously had zero test coverage | testing, notify |

| Roam #41 | `internal/dashboard/client.go` body drain: `Register`, `FetchNotifyConfig`, and `FetchServers` error paths now drain the response body via `io.Copy(io.Discard, ...)` before returning — without draining, `http.Transport` aborts the connection on non-2xx responses instead of returning it to the pool for reuse; `ReportState` and `RemoveServer` already did this correctly | correctness, performance |
| Roam #41 | Dashboard Google Fonts: Fraunces removed from the `<link>` URL — the dashboard CSS uses only Work Sans, DM Serif Display, and JetBrains Mono; Fraunces was loaded on every page load but never referenced in any CSS rule | perf, dashboard |

| Roam #42 | `internal/dashboard/client.go` body drain on JSON decode failure: `Register`, `FetchNotifyConfig`, and `FetchServers` decode-error paths now drain the body via `io.Copy(io.Discard, ...)` before returning — Roam #41 drained non-2xx error paths but a 2xx response whose body is only partially consumed by `json.Decoder` also prevents TCP keep-alive reuse without an explicit drain | correctness, performance |
| Roam #42 | Dashboard favicon `<link>` type corrected from `image/x-icon` to `image/png` — the embedded file is `favicon.png` served with `Content-Type: image/png`; the type attribute must match the actual MIME type | correctness, dashboard |
| Roam #42 | Dashboard server cards: unlimited-session display (`max_sessions == 0`) now shows "N active, M disconnected" when `disconnected_sessions > 0`, instead of only "N active" — disconnected sessions still consume resources and the full session picture is what admins need when assessing capacity on servers without a configured maximum | UX, dashboard |
| Roam #43 | Dashboard dynamic page title: `render()` updates `document.title` to "N alert · DrainCtl" when any servers are in Alert, "N grace · DrainCtl" when any are in Grace (but none Alert), or "DrainCtl Dashboard" when all healthy — the browser tab now conveys status at a glance without the window needing focus | UX, dashboard |
| Roam #43 | Dashboard Grace Left per-server grace period: server cards in Grace state now read `r.grace_period_seconds` from each server's last CheckResult (present in every report since the initial implementation) rather than the globally-fetched `graceMinutes`; falls back to `graceMinutes * 60` for older agent versions; Grace Left now displays immediately on first data load rather than waiting for the notify-config fetch | correctness, UX, dashboard |
| Roam #43 | Dashboard status pill on auth failure: a 401 or 403 response from `/api/v1/servers` now sets the status pill to "Auth Error" (amber) instead of leaving it at its previous stale "Live" value — operators can distinguish an authentication failure from a connectivity loss | correctness, UX, dashboard |
| Roam #44 | B5: landing page inline styles eliminated — `.section--shaded` (background + top/bottom border on `#how`, `#install`), `.section--bordered` (border-only on `#new`), `.install-cards` (3-col install grid), `.wrap--wide` (1200px), `.install-cta` (push-to-bottom CTA in install cards), `.btn--block` (full-width flex install buttons), `.btn-psgallery` (PSGallery blue), `.features-grid--2col` (dashboard 2-col features grid); `@media (max-width:640px)` fragile `[style*="grid"] !important` attribute selector replaced with `.install-cards` class rule | code quality, docs |
| Roam #44 | B9: `internal/dashboard/mock.js` → `testdata/mock.js`; `mock_dev.go` + `mock_prod.go` compile-time pair added — devmode builds (−tags devmode) embed `testdata/mock.js` and register `GET /mock.js` on the server mux so the `?mock` developer mode works end-to-end via the dev server; production builds compile a no-op stub | code quality, dashboard |
| Roam #45 | B4: Dashboard settings modal unsaved-changes guard — `_cfgDirty` flag tracks any modification to notification targets (type, URL, secret, trigger pills, repeat interval), grace period, or session-warning threshold; closing the modal via close button, overlay click, or Escape prompts "You have unsaved changes. Close without saving?" when dirty; flag resets on `loadNotifyConfig` open, after DOM population (so initial row population does not set dirty), and on successful save | UX, dashboard |
| Roam #46 | Dashboard settings modal inline → CSS: `style="display:grid;grid-template-columns:1fr 1fr;..."` replaced with `.settings-cfg-grid`; mobile media query updated from fragile `[style*="grid-template-columns"] !important` to plain `.settings-cfg-grid { grid-template-columns:1fr }` — same pattern eliminated for install cards in Roam #44; `.settings-group--flush`, `.settings-num-label`, `.repeat-pill--dashed`, `.repeat-pill-extra` added for remaining modal overrides; `.settings-num` CSS width corrected to 80px and all three inline `style="width:80px"` removed (static and dynamically generated); `margin-top:12px` moved into `.btn-add-target` CSS | code quality, dashboard |
| Roam #46 | Landing page inline → CSS: `.hero-ps` and `.ps-oneliner` + `.ps-oneliner code` CSS added for the hero PowerShell one-liner widget (removed three block inline-style attrs); `.section-title--italic` modifier replaces `style="font-style:italic"`; `.comic-body--row`, `.comic-icon--lg`, `.comic-text` for the wide comic panel flex layout; `.dashboard-ss` + `.dashboard-ss img` for the screenshot wrapper; `.section-cta` for the Dashboard Guide CTA row — zero inline styles remaining (except SVG `vertical-align:middle`) | code quality, docs |
| Roam #46 | Fixed two malformed `</` closing tags (missing `a`) in install-card anchor buttons — pre-existing bug that caused `prettier` to error out on every `just fmt-web` run since the Roam #44 pass; also corrected `justfile` `fmt-web` glob from `internal/dashboard/*.js` to `internal/dashboard/testdata/*.js` after mock.js was relocated in Roam #44 | correctness, code quality, docs |
| Roam #47 | `notify.go`: `for k := range state.LastAlertNotify { delete(...) }` → `clear(state.LastAlertNotify)` — idiomatic Go 1.21+ map reset (two maps are now separate, no filter needed); `internal/svc/check.go` `resetSessionWarnCooldown`: same `for/delete` → `clear(state.LastSessionWarnNotify)` | code quality |
| Roam #47 | `docs/guide.html` CLI Usage — added `--since`/`--until` RFC 3339 examples to the history section; added `drainctl configure` subsection covering both interactive wizard and flag-driven mode; Service Management — added `drainctl service status` line above `Get-Service DrainCtl` | docs |
| Roam #48 | `cmd/drainctl/main.go` (929 lines) split into 8 focused command-group files — `check_cmd.go` (103), `history_cmd.go` (111), `audit_cmd.go` (31), `notify_cmd.go` (143), `service_cmd.go` (91), `register_cmd.go` (83), `dashboard_cmd.go` (171), `configure_cmd.go` (214); `main.go` reduced to package setup + root wiring (53 lines) | code quality, code organization |
| Roam #48 | `runConfigureFlags` bug fix: webhook/ntfy URLs were appended unconditionally, so running `drainctl configure --webhook-url X` twice produced duplicate notification targets; replaced with `upsertNotifyTarget` helper (update first matching type or append new) shared with `setNotifyTarget` | correctness, CLI |
| Roam #49 | `drainctl configure` flags path: added `--session-warning-threshold` (0–100, 0=disabled), `--poll-interval` (seconds, ≥10), and `--retention-days` (1–365) — the interactive wizard already prompted for all three but MSI installer/scripted invocations could only set grace period | feature, CLI |
| Roam #49 | `drainctl check` plain output (service-pipe path): session data now printed between grace_period and the final status line — capped server: `sessions=N/M utilization=P%`; uncapped: `sessions=active=N[ disconnected=M]`; matches the information already present in the JSON/CSV paths | feature, CLI |
| Roam #49 | Dashboard event log `sessInfo` (unlimited servers): changed from `"N active, T total"` to `"N active[, M disconnected]"` — consistent with the server card added in Roam #42; `total_sessions == active + disconnected` so the old form was redundant when no cap is set | correctness, UX, dashboard |

| Roam #50 | `dc.Check()` direct path: session data now logged in plain output — capped server: `sessions=N/M utilization=P%`; uncapped: `sessions=active=N[ disconnected=M]`; matches the pipe path output added in Roam #49 (fallback path fetched sessions into `res.Sessions` but never logged them) | correctness, CLI |
| Roam #50 | `generateSelfSigned` TLS cert write: `pem.Encode` and `certOut.Close()` errors no longer silently ignored — on failure the partial cert file is removed before returning the error; previously a write failure left a corrupt empty cert on disk, causing `tls.LoadX509KeyPair` to fail on the next restart with no clear indication of the root cause | correctness, dashboard |

| Roam #51 | `Config.Validate`: `PollInterval` guard widened from `< 10` to `< 10 \|\| > MaxPollInterval` (`MaxPollInterval = 86400 s`) — a hand-edited config.json with an arbitrarily large poll interval was silently accepted, effectively halting state monitoring; `MaxPollInterval` exported so callers and tests can reference it; 2 new tests (`TestValidate_PollIntervalClamped`, `TestValidate_PollIntervalPreservesValid`) | correctness, config, testing |
| Roam #51 | Dashboard `renderHistoryModal` XSS consistency: transition branch now wraps `mode()` with `esc()` (`esc(mode(e.transition_from)) + " → " + esc(mode(e.drain_mode))`) — the non-transition branch already used `esc(mode(...))` but the transition branch injected the `mode()` result directly into `innerHTML`; drain-mode values are currently alphanumeric constants but defence-in-depth requires escaping all server-sourced strings | security, dashboard |

| Roam #52 | `SendNotification` webhook payload: added `grace_period_seconds` (configured alerting threshold in seconds), `connections_allowed` (boolean — true only when drain mode is off), and `version` (reporting agent version) — webhook consumers previously had to parse `status` to determine if connections were blocked and had no way to know the alerting threshold or compute Grace time remaining; 2 new tests (`TestSendNotification_WebhookPayloadContextFields`, `TestSendNotification_ConnectionsAllowedTrueWhenHealthy`) | feature, notify, testing |

| Roam #53 | `drainctl configure`: `dc.LoadConfig` failure now warns the user (`config unreadable (…); starting from defaults`) instead of silently falling back — operators running the wizard on a machine with a corrupted config.json would previously see defaults with no indication that their config was lost | correctness, UX, CLI |
| Roam #53 | `drainctl check` direct path: `source=direct` logged before the registry read — mirrors the `source=service` line emitted via the pipe path so both paths are identifiable in plain output | UX, CLI |
| Roam #53 | `drainctl notify status`: empty `Triggers` list (meaning DefaultTriggers apply) now prints `triggers=[drain_on,drain_off,alert,healthy] (default)` instead of `triggers=[]` — the blank list was misleading since notifications were still firing on the default set | correctness, UX, CLI |
| Roam #53 | `pipe.go` `handlePipeConn`: `json.Marshal` errors on status and history results now produce a `PipeResponse{OK:false, Error:…}` instead of silently sending `{ok:true,data:null}` — the client's `CheckViaPipe`/`HistoryViaPipe` would have failed with a JSON unmarshal error anyway, but now the error message is actionable | correctness, svc |

| Roam #54 | `Config.Validate`: `RepeatMinutes` clamped to `[0, MaxRepeatMinutes]` (10080 = 1 week) — negative values are normalised to 0 (once-only); values above `MaxRepeatMinutes` would overflow `time.Duration` when multiplied by `time.Minute`; `MaxRepeatMinutes` exported constant added; 3 new tests (`TestValidate_RepeatMinutesClampsNegative`, `_ClampsAboveMax`, `_PreservesValid`) | correctness, config, testing |
| Roam #54 | `handlePutNotifyConfig`: notification targets are now validated before calling `UpdateNotifySettings` — unknown `type` values, non-http/https URL schemes, unknown trigger names, and out-of-range `repeat_minutes` each return 400 Bad Request with an index-keyed message instead of being silently stripped by `Config.Validate()` after the save (which returned 200 OK with no indication that targets were dropped); 5 new tests (`TestHandlePutNotifyConfig_InvalidTargetType_Returns400`, `_InvalidTargetURLScheme_Returns400`, `_InvalidTargetTrigger_Returns400`, `_OutOfRangeRepeatMinutes_Returns400`, `_ClearNotificationsWithEmptyArray`) | correctness, security, dashboard, testing |

| Roam #55 | Dashboard `dashboard.html` inline style extraction: all static `style="..."` attributes in HTML and JS templates moved to named CSS classes — `.footer-brand` (JetBrains Mono nav brand), `.settings-actions` (save/test flex row), `.hist-head-actions` (history modal header right side), `.hist-head .filter-pill` scoped size override, `.srv-v--amber` (Grace Left amber value), `.chart-placeholder` (collecting-data overlay), `.ntfy-prefix` (static styles only; `display:` kept inline), `.webhook-secret-wrap` (static styles; `display:` kept inline), `.webhook-secret-wrap .settings-input` (`flex:1`), `.target-section-label--nowrap` modifier (HMAC Secret heading), `.hmac-header-label` (X-DrainCtl-Signature span), `.settings-num-label` reused for repeat interval unit; redundant `style="border-left-color:var(--accent)"` removed from `.ctr.total` (already set by CSS rule) | code quality, dashboard |

| Roam #56 | `drainctl notify status`: webhook targets now show `hmac_secret=set` or `hmac_secret=unset` — operators can confirm HMAC signing is active without reading config.json; ntfy targets do not show the field (HMAC signing is webhook-specific) | UX, CLI |
| Roam #56 | `docs/guide.html` troubleshooting: Kerberos 401 section used `http://server.domain.com:49470` as example FQDN; corrected to `https://` (dashboard always requires TLS) | correctness, docs |
| Roam #56 | `docs/guide.html` configure section: expanded flag example to show `--session-warning-threshold`, `--poll-interval`, `--retention-days` (all three were added in Roam #49 but not reflected in the guide) | docs |
| Roam #56 | `docs/guide.html` session utilization alerts: added `drainctl configure --session-warning-threshold N` as the primary way to change the threshold; config.json snippet retained as secondary reference | UX, docs |

| Roam #57 | `svcRunCheck` session-warning cooldown reset: narrowed `else` branch to `else if sess != nil && sess.MaxSessions > 0` — previously `resetSessionWarnCooldown` was called whenever the utilization condition was false, including when `sess==nil` (WTS API error); a transient query failure cleared the per-target cooldown so the next successful poll re-fired the warning even though sessions had not recovered | correctness, svc, notify |
| Roam #57 | `SendTestNotification`: switched from tracking `lastErr` to accumulating all per-target errors via `errors.Join` — callers now receive every failed-target URL in the error message instead of silently discarding all but the last failure; 1 new test `TestSendTestNotification_MultipleErrors_ReturnsAll` | correctness, notify, testing |
| Roam #57 | `handleUI` tests: 3 new tests in `server_test.go` (`TestHandleUI_ReturnsHTML`, `TestHandleUI_NoCacheHeader`, `TestHandleUI_BodyContainsDashboard`) — the SPA entry-point handler previously had zero test coverage | testing, dashboard |
| Roam #58 | Dashboard `.empty h2` CSS bug: `Fraunces` was removed from the Google Fonts link in Roam #55 but the CSS reference was not updated — "Connecting…", "Access Denied", and "No servers registered yet" headings fell back to the browser's generic serif; changed to `DM Serif Display` to match the dashboard design | correctness, dashboard |
| Roam #58 | 17 new tests in `config_test.go` — `Validate` SessionWarningThreshold clamping (negative → 0, > 100 → 100, valid 0–100 preserved); `HasTargets` (empty, with URL, empty URL, mixed); `ToServiceConfig` unit conversions (GracePeriod int-minutes → Duration, PollInterval int-seconds → Duration, field copies); `ToDashboardConfig` AutoPin nil/&true/&false semantics and field copies | testing |
| Roam #59 | 14 new tests in `format_test.go` covering three previously untested output-format functions: `WriteHistory` (JSON valid-JSON array + field checks, CSV header+data, Table columns+values, Plain log-format lines with ERR level for non-zero exit, Empty/no-panic across all formats); `WriteHistoryRecords` (same 5 coverage points for the pipe-path pre-computed variant, plus Plain_ErrorLevel confirming ERR vs INF level selection); `CheckResult.Write` (JSON round-trip via `json.Unmarshal`, CSV header+status/host/changed_by, Table HOST/STATUS column headers + values, Plain_NoOp confirming `FormatPlain` intentionally writes nothing) | testing |
| Roam #60 | `SendTestNotification` webhook payload: added `grace_period_seconds` (0), `connections_allowed` (true), and `version` (`Version` constant) — test payload was structurally different from the production payload (Roam #52 added these three fields to `SendNotification` but not to `SendTestNotification`); webhook consumers that validate schema against real notifications would receive a different field set for tests; 1 new test `TestSendTestNotification_WebhookPayloadSchemaComplete` (verifies all 11 required top-level fields are present, plus `event="test"`, `connections_allowed=true`, `grace_period_seconds=0`, and non-empty `version`) | correctness, notify, testing |
| Roam #60 | `docs/guide.html` webhook payload example: updated from 9 fields (pre-Roam #52) to full current schema — added `grace_period_seconds`, `connections_allowed`, `version`, and `sessions` object; added explanatory note that `previous_mode` is transition-only, `sessions` is monitoring-only, and `connections_allowed` is the recommended field to check for automation gating instead of parsing `status` | docs |

| Roam #61 | `drainctl sessions` — new subcommand lists current RDS sessions via `WTSEnumerateSessionsW`; shows session ID, username, station, and state; default format is table; supports JSON, CSV, and plain formats; `WriteSessions(w, sessions, summary, format)` + `formatSessionSummaryLine` added to `format.go` following the `WriteHistory` pattern; 13 new tests in `format_test.go` (JSON valid/empty, CSV header+data, table columns+values+summary, plain log lines, nil-summary omission, summary line capped/uncapped) | feature, CLI, testing |
| Roam #62 | `ComputeSessionSummary(sessions, maxSessions)` extracted from `GetSessionSummary` — eliminates double WTS API call in `sessions_cmd.go` (previously `EnumerateSessions` was called twice: once directly, once inside `GetSessionSummary`); pure function enables unit testing without syscall mocks; `GetSessionSummary` now delegates to it; 7 new tests in `sessions_test.go` covering `wtsStateName` (all 10 named states + unknown fallback) and `ComputeSessionSummary` (empty list, active+disconnected counts, non-counted states ignored, utilization capped at 100%, no max); dashboard favicon MIME type corrected from `image/png` to `image/x-icon` | correctness, testing, dashboard |
| Roam #63 | `docs/guide.html` CLI Usage: added "List current sessions" subsection after "View audit history" — shows `drainctl sessions` with `--format json`/`--format csv` examples; guide Section 8 Session Tracking: added "drainctl sessions" subsection as the first h3 with example table output and format notes — `drainctl sessions` (added in Roam #61) was entirely absent from the guide | docs |
| Roam #64 | `AuditRecord` + `HistoryRecord`: added `disconnected_sessions` field (`omitempty`, backward-compatible) — disconnected session counts were tracked in `SessionSummary` and logged in CLI/dashboard output but never persisted to the JSONL audit trail or included in the history data structures; `check.go` and `internal/svc/check.go` record `DisconnectedSessions` when sessions are available; `WriteHistory` and `WriteHistoryRecords` CSV output updated to include `active_sessions`, `disconnected_sessions`, `total_sessions`, `max_sessions` columns (previously all session data was omitted from CSV history exports); 3 new tests (`TestAuditToHistory_CopiesDisconnectedSessions`, `TestWriteHistory_CSV_IncludesSessionColumns`, `TestWriteHistoryRecords_CSV_IncludesSessionColumns`) | correctness, testing |
| Roam #65 | `drainctl configure show` — new read-only subcommand prints all current settings without prompting: version + config path, grace period, poll interval, retention days, session warning threshold, dashboard server settings (enabled/port/group), dashboard agent registration URL + TLS pin status, and all notification targets; fills the operator gap where full config inspection required running the interactive wizard or reading config.json directly; `printNotifyTargets` extracted from the `notify status` inline body into a shared helper so both commands produce identical notification output | feature, UX, CLI, code quality |
| Roam #66 | `ntfyTitle(trigger, host)` helper — replaces raw trigger-name format (`"DrainCtl: drain_on on HOSTNAME"`) with readable labels: Drain Mode Active, Connections Restored, Grace Period Active, Alert: Drain Exceeded Grace Period, All Connections Allowed, Session Utilization Warning; unknown triggers fall back to raw name; `docs/guide.html` updated with `drainctl configure show` in Section 5 (CLI Usage) and Section 3 (Configuration) — command was added in Roam #65 but not documented; 12 new tests (`TestNtfyTitle` 7 sub-tests, `TestSendNotification_NtfyTitleIsReadable` 5 sub-tests) | UX, notify, testing, docs |
| Roam #67 | `WriteSessions` plain format bug fix: station field was silently dropped whenever a `UserName` was present — convoluted `append(fields[1:]...)` + overwrite pattern prepended `user=X` then replaced station with session_id, producing `user session_id state` instead of `user session_id station state`; fixed to `append([]string{user=X}, fields...)` so all four fields are always emitted; `WriteHistoryRecords` table format: raw RFC3339 timestamps (`2026-04-01T10:01:00+00:00`) reformatted to `2006-01-02 15:04:05` matching `WriteHistory` table — `drainctl history --format table` is now visually identical on the pipe path (service running) and direct path (fallback); `joinFields` private helper removed in favour of `strings.Join(fields, " ")`; tests updated (`TestJoinFields_*` removed, `TestWriteSessions_Plain_LogLines` asserts station + session_id presence, `TestWriteHistoryRecords_Table` asserts absence of RFC3339 `T` separator) | correctness, UX, code quality, testing |
| Roam #68 | `ChangesFiltered` ring buffer/limit path coverage: `n>0` branch was 0% tested (overall 69.6%); 3 new sub-tests cover `limit_applied_returns_newest_n_transitions` (n=1), `limit_with_time_filter` (n=2 + `since` bound), and `ring_buffer_evicts_oldest_when_over_limit` (3 transitions, limit=2); coverage 69.6% → 95.7%. `CheckResult.Write` nil-pointer paths: `formatTimePtr`/`formatFloatPtr` nil branches were never hit (all existing tests provided non-nil `StateSince`/`StateDurationSeconds`); `TestCheckResultWrite_CSV_NilPointers` asserts nil pointers produce empty CSV columns with no `<nil>` literal; `TestCheckResultWrite_Table_NilDuration` asserts nil duration renders as `"n/a"`; `formatTimePtr`, `formatFloatPtr`, `formatAge` all reach 100%. `audit.go` CRLF normalisation (`gofmt -w .`) — Windows git checkout had re-introduced line-ending drift, breaking `just lint`. | testing, code quality |
| Roam #69 | `CheckResult.Write` CSV gains 4 session columns (`active_sessions`, `disconnected_sessions`, `total_sessions`, `max_sessions`) — history CSV already included these fields (Roam #64) but the check CSV omitted them, leaving a data gap for anyone exporting check results; nil `Sessions` produces empty fields (backward-compatible — new columns appended at end); 3 new tests (`TestCheckResultWrite_CSV_WithSessions`, `TestCheckResultWrite_CSV_NilSessionsAreEmpty`, header assertions added to existing `TestCheckResultWrite_CSV`). Coverage sweep: `handleHealth` `case "Grace": grace++` and `default: unknown++` branches were untested — 2 new tests (`TestHandleHealth_GraceServerCounted`, `TestHandleHealth_ErrorStatusCountedAsUnknown`) push `handleHealth` to 100%; `formatSessionSummaryLine(nil)` nil path and plain-format nil-summary path tested by `TestFormatSessionSummaryLine_Nil` + `TestWriteSessions_Plain_NilSummary` → `formatSessionSummaryLine` 88.9% → 100%; `Config.Validate` `AuditPath == ""` branch covered by `TestValidate_SetsDefaultAuditPath` + `TestValidate_PreservesExistingAuditPath` → `Validate` 95.5% → 97.7%. | feature, testing, correctness |
| Roam #70 | `drainctl notify set-webhook` gains `--secret`, `--triggers`, `--repeat-minutes` flags; `set-ntfy` gains `--triggers` and `--repeat-minutes` — previously only the URL could be set from CLI; configuring HMAC signing, trigger filters, or repeat intervals required the dashboard UI or manual config.json edits, blocking scripted/MSI deployments. `notifyOverrides` struct with nil-pointer semantics (nil = preserve existing value); `parseTriggers` validates comma-separated names against `ValidTriggers`; overrides applied after `upsertNotifyTarget` in a single save. `triggerList()` produces a sorted, deterministic list for help text and error messages. `docs/guide.html` §5 (CLI Notifications) and §7 (Notifications setup) updated with flag descriptions and examples. | feature, CLI, UX, docs |
| Roam #71 | `TestSendNotification_ResetsOnDrainOff`: verifies `TriggerDrainOff` clears `LastAlertNotify` (same as `TriggerHealthy`) so the alert re-fires correctly on the next cycle; `TestSendTestNotification_UnknownTypeSkipped`: documents that unknown target types with non-empty URLs are silently skipped (forward-compatibility contract); `TestRateLimiter_StaleBucketPruned`: exercises the lazy prune loop in `ipRateLimiter.Allow` by artificially aging a bucket past `pruneAfter` and asserting it is deleted on the next Allow call; `triggerList()` simplified — manual insertion sort replaced with `slices.Sort`, copy-then-sort pattern eliminated. | testing, code quality |
| Roam #72 | Coverage sweep — 10 new tests: `TestSendNotification_PreviousModeInPayload` / `_NoPreviousModeWhenNotTransition` — the `previous_mode` webhook payload field was untested; test verifies field present when `Transition=true` with `TransitionFrom` set, and absent when `Transition=false`; `TestSendWebhook_InvalidURL_ReturnsError` / `TestSendNtfy_InvalidURL_ReturnsError` — `http.NewRequest` error path in both notification backends covered via null-byte URL; `TestSendTestNotification_NtfyError_ReturnsError` — ntfy server returning 503 yields error containing the target URL; `TestAuditStore_Close` — no-op `Close()` interface method documented and covered; `TestHandleRegister_AuthenticatedUser_Returns200` / `TestHandlePutNotifyConfig_AuthenticatedUser_Returns200` / `TestHandleNotifyTest_AuthenticatedUser_Returns200` — `auth != nil` username-logging branch in all three handlers covered by injecting `AuthInfo` via `context.WithValue(r.Context(), authInfoKey, ...)` (same-package test access). Root: 53.0% → 53.6%; dashboard: 42.6% → 43.2%. | testing |
| Roam #73 | `GET /api/v1/servers/{host}` — new endpoint returns `ServerInfo` JSON for a single registered host (group-auth required); completes the REST design alongside `GET /api/v1/servers` (all) and `DELETE /api/v1/servers/{host}`; `ServerState.Get(hostname string) *ServerInfo` added to `internal/dashboard/store.go`; 3 new tests (`TestHandleGetServer_ReturnsServerInfo`, `_UnknownHostReturns404`, `_NoLastResultReturnsRegisteredHost`). Coverage sweep — 3 new tests: `TestHandleReport_ReadBodyError_Returns400` / `TestHandlePutNotifyConfig_ReadBodyError_Returns400` — `io.ReadAll` failure branch in both handlers was uncovered; `errReader` type in `server_test.go` simulates a read error; `handleReport` 88.9% → 100%; `handlePutNotifyConfig` 87.5% → 91.7%. `TestSendWebhook_MarshalError_ReturnsError` — `json.Marshal` error path was effectively dead for normal map values; `math.NaN()` triggers `UnsupportedValueError` and covers the path; `sendWebhook` 89.5% → 94.7%. Dashboard 43.2% → 44.9%. | feature, dashboard, testing |
| Roam #74 | Coverage sweep — 4 new tests: `TestHandleRegister_ReadBodyError_Returns400` — `io.ReadAll` failure branch in `handleRegister` was uncovered; `handleRegister` 90.9% → 100%. `TestHandleGetServer_EmptyHost_Returns400` — empty `PathValue("host")` guard was untested; `handleGetServer` 83.3% → 100%. `TestHandleNotifyTest_NilTestFuncUsesLoadConfigFunc_Success` / `_LoadError` — `testNotifyFunc==nil` path called `dc.LoadConfig` directly, making it untestable; refactored to delegate to `testLoadConfigFunc` when set (same pattern as `handleGetNotifyConfig`); two new tests exercise the success and load-error paths; `handleNotifyTest` 72.2% → 90.9%. Dashboard package 44.9% → 46.2%. All tests pass, lint clean. | testing, dashboard |
| Roam #76 | Coverage sweep — 9 new tests across 3 files: `TestOpenAuditStore_MkdirAllError` — `os.MkdirAll` failure in `OpenAuditStore`; `OpenAuditStore` 75.0% → 100%. `TestRecord_OpenFileError` — `os.OpenFile` failure in `Record` (directory at audit path); `Record` 80.0% → 90.0%. `TestScanRecords_EmptyLineSkipped` — `len(line)==0 continue` branch in `scanRecords`; `scanRecords` 84.2% → 89.5%. `TestHistory_ScanError` — `return nil, err` in `History` via >64 KiB line triggering `bufio.ErrTooLong`; `History` 94.1% → 100%. `TestPrune_ScanError` / `TestPrune_CreateTempError` — `scanRecords` error and `os.Create` failure in `Prune`; `Prune` 70.6% → 76.5%. `TestClampRetention_LogsWarning_AboveMax` — `log != nil` branch for above-max in `ClampRetention`; `ClampRetention` 88.9% → 100%. `TestSave_MkdirAllError` / `TestSave_MarshalError` — `os.MkdirAll` and `json.MarshalIndent` failure in dashboard `save()` (NaN injected via `LastResult.StateDurationSeconds`); `save` 75.0% → 100%. Root: 54.2% → 54.9%; dashboard store: `save` 75.0% → 100%. All tests pass, lint clean. | testing |
| Roam #77 | Coverage sweep — 6 new tests across 2 files: `TestHistoryFiltered_ScanError` / `TestChangesFiltered_ScanError` / `TestChanges_ScanError` — `return nil, err` scanner-error propagation in three `AuditStore` filter functions; oversized-line trigger (`bufio.ErrTooLong`); `HistoryFiltered` 95.2% → 100%, `ChangesFiltered` 95.7% → 100%, `Changes` 93.8% → 100%. `TestScanRecords_OpenError` — `return fmt.Errorf("open audit file: …")` branch when `os.Open` fails with non-`IsNotExist` error; null byte in path produces `syscall.EINVAL` (not `os.IsNotExist`) on Windows; `scanRecords` 89.5% → 94.7%. `TestHandlePipeConn_ConnectionClosed` — `err != nil || n == 0` early return in `handlePipeConn` when client closes before sending data; `handlePipeConn` 91.4% → 97.1%. `TestHandlePipeConn_StatusMarshalError` — `json.Marshal` error path in the `"status"` case; `math.NaN()` in `CheckResult.StateDurationSeconds` triggers `json.UnsupportedValueError`; response is `{ok:false, error:"marshal result:…"}`. Root: 54.9% → 55.2%; pipe: 21.7% → 23.0%. All tests pass, lint clean. | testing |

| Roam #78 | Coverage sweep — 3 new tests across 2 files: `TestValidate_InvalidURLSchemeLogsWarning` — `log != nil` warn path in `Config.Validate` when a notification target carries an invalid URL scheme (ftp:// etc.) and a non-nil log is supplied; `Validate` 97.7% → 100%. `TestDefaultDataDir_FallsBackWhenProgramDataEmpty` — `pd = "C:\ProgramData"` fallback in `DefaultDataDir` when `ProgramData` env var is unset (`t.Setenv("ProgramData","")` restores automatically); `DefaultDataDir` 75.0% → 100%. `TestPrune_RenameError` — `os.Rename` failure in `AuditStore.Prune` exercised via `windows.CreateFile` with no `FILE_SHARE_DELETE` on the destination file so `MoveFileEx` returns a sharing violation; covers the rename error path and `os.Remove(tmp)` cleanup call; `Prune` 76.5% → 82.4%. Root: 55.2% → 55.6%. All tests pass, lint clean. | testing |

| Roam #79 | Coverage sweep — 4 new tests across 2 files: `TestScanRecords_EarlyStop` — `if !fn(rec) { break }` early-stop path in `scanRecords` was never exercised because all production callers return true from their callbacks; direct white-box test passes a callback that returns false after the first record; `scanRecords` 94.7% → 100%. `TestFlushIfDirty_FlushesWhenDirty` — `return m.Flush()` path in `FlushIfDirty` was untested (only the `dirty==0` no-op path existed); append one record then call `FlushIfDirty`, verify file grows; `FlushIfDirty` 83.3% → 100%. `TestClose_IdempotentOnSecondCall` — `if m.file == nil { return nil }` guard in `Close` was untested; call Close twice on the same store; `Close` 87.5% → 100%. `TestOpenMemAuditStore_LoadError_OversizedLine` — `return nil, fmt.Errorf("load audit file: %w", err)` path in `OpenMemAuditStore` was untested; write a >64 KiB line to trigger `bufio.ErrTooLong` inside `load`, covering both the `load` scanner-error return and the `OpenMemAuditStore` load-failure return; `OpenMemAuditStore` 66.7% → 77.8%. Root: 55.6% → 55.7%; store: 85.8% → 88.8%. All tests pass, lint clean. | testing |

| Roam #80 | Coverage sweep — 6 new tests across 2 files: `TestOpenMemAuditStore_NilLog` — nil log guard in `OpenMemAuditStore` assigns `dc.DiscardLogger()`; all prior callers passed non-nil so the assignment was dead; `OpenMemAuditStore` 77.8% → 88.9%. `TestOpenMemAuditStore_MkdirAllError` — regular file at the would-be directory path causes `os.MkdirAll` to fail; covers the error return; store 88.8% → 90.3%. `TestHandleGetNotifyConfig_ProductionPathLoadsConfig` — `testLoadConfigFunc==nil` path calls `dc.LoadConfig` directly; `t.Setenv("ProgramData", dir)` redirects I/O to a temp dir; `handleGetNotifyConfig` 94.1% → 100%. `TestHandleNotifyTest_BothHooksNil_UsesProductionLoad` — both `testNotifyFunc` and `testLoadConfigFunc` nil; inner `loadFn==nil` guard builds a real `dc.LoadConfig` closure; `handleNotifyTest` 90.9% → 100%. `TestHandlePutNotifyConfig_ProductionPathUpdatesConfig` — `testPutNotifyConfigFunc==nil` path calls `dc.UpdateNotifySettings` successfully; covers success path of else block. `TestHandlePutNotifyConfig_ProductionPath_UpdateError_Returns500` — directory at `config.json` causes non-`os.IsNotExist` error inside `UpdateNotifySettings`; handler returns 500; `handlePutNotifyConfig` 91.7% → 100%. dashboard 47.4% → 48.4%. All tests pass, lint clean. | testing |
| Roam #83 | `cmd/drainctl` first test file — `notify_cmd_test.go` (18 tests) covering 4 pure helpers at 0% coverage: `parseTriggers` (6 tests: valid list, single, whitespace trim, empty-part skip, unknown trigger error, all-empty error), `triggerList` (2 tests: all ValidTriggers present, lexicographic sort), `upsertNotifyTarget` (4 tests: update existing, append new, preserve other types, new target gets DefaultTriggers), `printNotifyTargets` (6 tests: empty → disabled warning, webhook+secret → hmac_secret=set, webhook−secret → hmac_secret=unset, ntfy → no hmac field, empty Triggers → (default), hasTargets=false → disabled). `logCapture` helper implements `dc.LogFunc` signature inline. `parseTriggers` / `triggerList` / `upsertNotifyTarget` / `printNotifyTargets` all 0% → 100%; `cmd/drainctl` 0.0% → 7.5%. All tests pass, lint clean. | testing |
| Roam #82 | Coverage sweep + correctness fix: (1) `internal/dashboard/tls_test.go` (new, 7 tests) — `generateSelfSigned` cert-write-error path; `CertFingerprint` no-file / invalid-PEM / invalid-DER error paths; `loadOrGenerateTLS` generate + reuse paths (skipped when not elevated via `isElevated()` helper); dashboard 49.7% → 52.6%. (2) `internal/svc/mgmt_test.go` (new, 2 tests) — `svcStateString` all 7 named states + `default: "Unknown(N)"` fallback; `svcStateString` 0% → 100%; svc 8.1% → 10.3%. (3) 3 new `memstore` tests — `TestLoad_EmptyLineSkipped` covers `len(line)==0 continue` in `load`; `TestFlushLocked_SeekError` covers `m.file.Seek(0,2)` error in `flushLocked` by closing the OS handle; `TestPrune_SeekError` covers `m.file.Seek(0,0)` error in `Prune`; store 90.3% → 92.5%. (4) `SetRecoveryActions` correctness fix — `_ = s.SetRecoveryActions(...)` changed to log `LvlWRN` on failure; previously silent errors would leave the service without auto-restart on crash. All tests pass, lint clean. | testing, correctness, svc |
| Roam #81 | Security coverage + dead-code fix — two changes: (1) `internal/dashboard/client_test.go` (new file, 12 tests) — `newDashClient`'s `VerifyPeerCertificate` callback was security-critical code at 0% coverage; tests call the callback directly with in-memory certificates generated via `ecdsa.GenerateKey`/`x509.CreateCertificate`; covers all three branches (empty cert list → `"no TLS certificate presented"`, fingerprint mismatch → `"fingerprint mismatch"` error containing both actual and expected values, correct fingerprint → nil); also tests that empty-fingerprint clients omit the callback (TOFU mode), that the leaf cert alone is checked when multiple certs are presented, and that `InitDashClient` replaces the package-level atomic client; `newDashClient` 27.3% → 100%, `init`/`InitDashClient` also reach 100%; dashboard 48.4% → 49.7%. (2) `backoffDuration` dead-code fix — `d > configFetchMax` was unreachable: with `configFetchBase=5min` and shift capped at 5, `d = 5min × 32 = 160min = configFetchMax` (equal, never greater); changed `>` to `>=` so the cap guard executes for failures≥5; added `{-1, 5min}` (negative input), `{1000, 160min}` (large input), and `TestBackoffDuration_NeverExceedsMax` (invariant test over 0–20 failures) to `backoff_test.go`; `backoffDuration` 88.9% → 100%. All tests pass, lint clean. | testing, code quality |

| Roam #84 | Coverage sweep — new `log_test.go` (12 tests) and 2 new tests in `config_test.go`: `DefaultLogger` — output format (RFC3339 timestamp, `[LVL]` tag, space-joined fields, trailing newline), all four level strings (`[INF]`, `[WRN]`, `[ERR]`, `[OK ]`), quiet mode (suppresses INF+WRN, passes OK+ERR), multiple-field join; `DiscardLogger` — does not panic on any input; `LogMsg` — wraps message as `msg="..."` quoted field, appends extra kv pairs, no-extra-fields path; `DefaultConfigPath` — ends with `\config.json`, starts with `DefaultDataDir()`. All four functions were at 0% coverage (no `log_test.go` existed); now `DefaultLogger` / `DiscardLogger` / `LogMsg` / `DefaultConfigPath` all 100%. Root: 55.7% → 56.3%. All tests pass, lint clean. | testing |

## Remaining Work

*(All tracked items complete — nothing pending after Roam #84.)*

## Key Learnings

- **Testing `VerifyPeerCertificate` callbacks without a live TLS connection**: Extract the callback via `client.Transport.(*http.Transport).TLSClientConfig.VerifyPeerCertificate` and call it directly with in-memory `[][]byte{certDER}`. Generate the DER bytes with `x509.CreateCertificate(rand.Reader, template, template, &key.PublicKey, key)`; compute the expected fingerprint with `hex.EncodeToString(sha256.Sum256(der)[:])`. This pattern fully exercises the pinning logic — empty-cert, mismatch, and match branches — without a network round-trip.
- **Dead backoff guard pattern**: When a function doubles a duration with a shift cap (e.g. `d = base << shift` with `shift` capped at N), the overflow guard `if d > max` may be unreachable if `base << N == max` exactly. Change `>` to `>=` so the guard fires for the equal case and is no longer dead code; behaviour is identical (still returns `max`) but the branch becomes exercisable.
- **Cobra `Changed()` for partial-update CLI flags**: When a command should update only the fields whose flags were explicitly passed (leaving others unchanged), use `cmd.Flags().Changed("flag-name")` rather than checking the value. A nil-pointer-per-field struct (`notifyOverrides`) conveys "not specified" cleanly without sentinel values, and avoids the common mistake of overwriting stored config with zero/default values for unset flags.
- **Zero tests existed** prior to this session. All Go files carry `//go:build windows`; test files need the same tag. The root package contains enough pure logic (config, notify, format) to be tested without syscall mocks.
- **Webhook signing pattern**: `X-DrainCtl-Signature: sha256=<hmac>` matches GitHub-style webhook signatures. Secret stored in `NotificationTarget.Secret`; empty secret = no header (backwards-compatible).
- **Health endpoint design**: Placed at `GET /api/v1/health` with no auth so load balancers and uptime monitors can probe without Kerberos. Returns `ok`, `version`, `servers`, `healthy`, `alerting`, `unknown` counts.
- **URL scheme validation**: Added to `Config.Validate` so it applies uniformly on every config load/save path. Empty URLs are preserved (existing behaviour handles them downstream).
- **`svc/handler.go` is large** (800+ lines) — split into sub-files would improve navigability but isn't blocking anything yet.
- **Dashboard backoff is now wall-clock-based**: `backoffDuration(n)` returns `time.Duration` (5 min base, doubles per failure, 160 min cap); `lastConfigFetch time.Time` compared via `time.Since()`. This is independent of `PollInterval` changes at runtime, unlike the old poll-count approach.
- **pflag flag detection**: `cmd.Flags().NFlag()` counts explicitly set flags (cobra/pflag); `NChanged()` does not exist. The pattern `NFlag() == 0` reliably separates interactive from scripted invocation.
- **History ring is in-memory only** — `ServerState.history` is not persisted to `servers.json`. It resets on service restart. This keeps the implementation simple; records reaccumulate as reports arrive. Persisting 100×N `CheckResult` JSON per host would bloat `servers.json` significantly.
- **Dashboard handler tests need no `devmode` tag**: handlers are methods on `*DashboardServer`; calling them directly with `httptest.NewRecorder` bypasses all middleware. Auth is only injected by the mux wrappers, not by the handlers themselves.
- **`audit.go` streaming pattern**: `scanRecords(fn func(AuditRecord) bool) error` replaces `readAll`. `History(n)` uses a copy-shift ring buffer — `O(n)` memory regardless of file size. `Changes(n)` uses the same ring. `StateSince` still collects all records (needs backward walk). `LastObservation` retains a single record.
- **SSPI `RevertToSelf` placement**: Calling `RevertToSelf` immediately after `isGroupMember` (before any early return) ensures cleanup in both error and success paths. If no impersonation happened, `RevertToSelf` is a no-op.
- **`NotifyState.LastAlertNotify` keyed by URL**: On config hot-reload, prune stale entries by building an active-URL set and deleting entries not in it. Only prune when using local config (not dashboard remote), since remote config manages its own targets.
- **`HistoryFiltered` ring buffer with time filter**: The scan is oldest-first; applying the time predicate before the ring-shift means only matching records count toward the limit. The pipe path returns `[]HistoryRecord` (string timestamps), so time filtering there is done post-fetch with RFC3339 parsing.
- **SCM start/stop polling pattern**: After calling `s.Start()` or `s.Control(svc.Stop)`, poll `s.Query()` every 500 ms up to 30 s. The initial `Control(svc.Stop)` response already carries the new status, so check it before sleeping. `ServiceStatus()` doesn't require admin — opening a service for `SERVICE_QUERY_STATUS` access is unprivileged on default Windows ACLs.
- **handler.go split boundary**: `handler.go` = service lifecycle (Execute loop, serviceHandler pipe bridge, RunService, backoffTicks, event log constants). `check.go` = one check cycle (svcRunCheck, pruneNotifyState, applyRemoteConfig). Both files need all the same imports because they share the same package and cross-reference each other's symbols.
- **MemAuditStore test strategy**: `OpenMemAuditStore` uses `windows.CreateFile` for exclusive locking, so tests require a real temp file (use `t.TempDir()`). The public methods (Append, History, Changes, StateSince, Flush, Prune) are pure in-memory logic after that; no mocking needed. The concurrent access test (`sync.WaitGroup` + multiple goroutines) exercises the `sync.RWMutex` under `go test -race`.
- **AuditStore test strategy**: `writeTestRecords` helper (from `audit_filter_test.go`) creates a real temp file, writes records, and returns the store — usable in `audit_test.go` without redeclaration (same package). `scanRecords` malformed-line skip can be tested by inserting raw garbage bytes via `os.OpenFile` after calling `Record`. `StateSince` for an absent mode returns `nil` (the backward walk finds no preceding different-mode record), while `History(0)` with `n=0` returns all records.
- **Security headers layering**: `securityMiddleware` (defensive headers: nosniff, SAMEORIGIN, Referrer-Policy, Permissions-Policy) wraps the mux unconditionally; `hstsMiddleware` wraps the result only when TLS is active. This keeps the two concerns separate and lets HSTS remain TLS-gated.
- **Health endpoint Grace miscounting**: `switch s.LastResult.Status { case "Alert": ... default: healthy++ }` silently lumped Grace, Error, and any unknown status into `healthy`. Fix: add explicit `case "Grace": grace++` and expose a `grace` JSON field. Adding a new field to a JSON response is backward-compatible — existing decoders ignore unknown fields.
- **PathValue fallback is dead code**: `DELETE /api/v1/servers/{host}` registered via Go 1.22 `http.ServeMux` always populates `r.PathValue("host")` when the route matches; the URL-split fallback can never be reached and should be deleted.
- **`ServerState` history is separate from `servers` map**: `Update()` appends to `history[hostname]` regardless of whether the host is registered; only the `ServerInfo` side is guarded by `IsRegistered`. Tests for `Update` on an unregistered host must assert that `All()` remains empty (no `ServerInfo` created), while accepting that the ring buffer will have an entry. The ring is always in-memory and not persisted, so this is benign.
- **`All()` returns value copies**: `ServerState.All()` copies each `*ServerInfo` by value before returning, so mutating the returned slice does not affect internal state. This is worth verifying explicitly (see `TestAll_ReturnsSnapshot`).
- **`hostname()` helper in store_test.go**: Uses `string(rune('A'+n))` to generate `SRVA`…`SRVE` for concurrent tests. Simple enough to avoid an import; works for up to 26 hosts.
- **`handleNotifyTest` uses `testNotifyFunc` injection**: `handleGetNotifyConfig` and `handlePutNotifyConfig` call `dc.LoadConfig` directly and have no unit tests (they require ProgramData). For `handleNotifyTest`, a `testNotifyFunc func() error` field on `DashboardServer` lets tests inject a fake function without touching the filesystem. The nil check keeps the production path clean.
- **Webhook secret in settings UI**: The `secret` field is only included in the `PUT /api/v1/notify-config` payload when non-empty (saves bandwidth and avoids accidentally clearing secrets when the user hasn't changed them). The password input uses `autocomplete="new-password"` to prevent browser autofill pollution.
- **`testLoadConfigFunc` / `testPutNotifyConfigFunc` injection pattern**: Extends the `testNotifyFunc` approach to config handlers. `testLoadConfigFunc func() (*dc.Config, error)` mirrors `dc.LoadConfig`'s signature but drops the `LogFunc` arg (captured by the closure in the real path). `testPutNotifyConfigFunc func([]dc.NotificationTarget, *int, *int) error` collapses the three real update calls (UpdateNotifications, UpdateSessionThreshold, UpdateGracePeriod) into one function — the nil pointer arguments signal "field was absent from request", matching the production nil-check guard.
- **CSP for embedded SPA**: `default-src 'none'` + per-directive allowlists is more restrictive than `default-src 'self'`. The dashboard's inline uPlot script and `<style>` blocks require `'unsafe-inline'` for both `script-src` and `style-src`; a nonce-based approach would require per-request HTML template rendering. `frame-ancestors 'self'` is redundant with `X-Frame-Options: SAMEORIGIN` but takes precedence in browsers that support CSP Level 2+; both are kept for compatibility.
- **Server filter bar is purely client-side**: All filtering is done by toggling `.hidden` on `.srv` card divs after each keypress or pill click. No API call needed — `render()` re-runs `filterGrid()` after each refresh so the active filter is reapplied automatically on new data. The "no match" notice is lazily created on first need and reused thereafter.
- **Event delegation over inline onclick for generated HTML**: Using a single `click` listener on `#grid` with `e.target.closest(".btn-hist")` / `".btn-rm"` avoids JS string interpolation of server-controlled hostnames in attribute context, eliminates one re-registration concern per re-render, and keeps the handler wiring out of the HTML generation loop.
- **Dashboard accessibility pattern**: `role="dialog" aria-modal="true" aria-labelledby="…"` on overlay divs; `aria-label` on icon-only buttons; `role="log" aria-live="polite"` on the event log; `role="img" aria-label="…"` on the visual-only state bar (updated dynamically from render). Focus management: move focus to the close button on open, return to the trigger on close, using `.focus()` in `toggleSettings()`.
- **Mobile nav dropdown from sticky parent**: A `position: sticky` element acts as the containing block for `position: absolute` children, so `top: 56px` (nav height) correctly places the dropdown flush below the sticky nav bar without any extra wrapper.
- **Grace remaining calculation is client-side only**: `state_duration_seconds` (from last poll) minus `grace_period * 60` (cached from config fetch) gives remaining grace time at poll time. It does not count down between polls; refreshing gets the latest value. Initialise `graceMinutes = null` so the row hides itself until the config has actually loaded — avoids showing stale "0 left" on first render.
- **History modal z-index ordering**: Settings overlay is z-index 150; log expanded is z-index 200; history modal uses z-index 160 so it can stack over settings but not over the log fullscreen view. ESC priority matches visual stack: history first, then settings, then log collapse.
- **`encodeURIComponent` for history hostname**: The `/api/v1/history/{host}` path value accepts hostnames with dots and hyphens; `encodeURIComponent` encodes these safely even for edge cases like hostnames containing `%` or `#`.
- **`NotifyState` shared-map bug**: Both `TriggerAlert` and `TriggerSessionWarning` originally stored repeat-tracking in `LastAlertNotify[target.URL]`. The cleanup on `TriggerHealthy` tried to preserve entries where `k == "session_warning"`, but `k` is a URL, so the guard was always false and all entries were deleted. Fix: split into `LastAlertNotify` (cleared on healthy) and `LastSessionWarnNotify` (never cleared). Writing a test that expected preservation exposed the bug immediately.
- **`Config.Validate` port guard**: `== 0` only catches zero — hand-edited config files can have negative or `> 65535` values. Use `< 1 || > 65535` to reject the full invalid range. The service would otherwise silently accept the value and fail on bind, producing a cryptic "address already in use" or "invalid argument" error.
- **Whitespace-only strings bypass empty checks**: `strings.TrimSpace` must be called *before* the `== ""` guard whenever the field comes from user-controlled input (JSON, CLI, config file). An all-whitespace `Dashboard.Group` passes the empty guard but causes silent SSPI group matching failures — the service starts but no user is ever authenticated.
- **`parseInt("0") || default` is a falsy-zero trap**: In JS, `parseInt("0") || 80` evaluates to `80` because `0` is falsy. The "Disable session warnings" pill sets the input to `"0"`, so saving would silently send `80` instead. Always use explicit NaN/range checks (`isNaN(v) || v < min || v > max`) rather than `|| default` fallbacks for numeric inputs.
- **String-concatenated JSON in CGo is a JSON-injection risk**: `C.CString(`{"error":"` + err.Error() + `"}`)` produces invalid JSON when the error contains `"` or `\`. Always use `json.Marshal(map[string]string{"error": err.Error()})` — the same `marshalError` helper used everywhere else.
- **Eager vs lazy map initialisation**: Both maps in `NotifyState` should be initialised at construction time (service startup) rather than lazily inside `SendNotification`. Lazy initialisation is correct but harder to reason about and adds a nil-check branch on every hot path. Initialise once at the call site that owns the object.
- **Flexbox `min-width:0` for text-overflow**: A flex item will not shrink below its content's natural width by default. Adding `min-width:0` allows the item to shrink, making `overflow:hidden` + `text-overflow:ellipsis` effective. Without it, long hostnames silently overflow the card header even with an ellipsis rule in place.
- **Modal focus restoration pattern**: Save the trigger element as `let _histTrigger = null` before opening the modal; restore focus in the close function. The history modal previously fell back to `<body>` focus on close, breaking keyboard navigation. The settings modal already did this correctly via `document.getElementById("btn-gear").focus()`.
- **Filter pill `aria-pressed` must be kept in sync**: Static HTML `aria-pressed="true"` on the default pill and `"false"` on others must be updated dynamically by the JS click handler. A `activateFilterPill()` helper that clears all pills then sets the active one avoids drift between the visual `.active` class and the ARIA attribute.
- **RFC 1123 hostname regex**: Single-char label `"a"` matches `^[a-zA-Z0-9]` with the optional trailing group absent; two-char label `"ab"` matches `[a-zA-Z0-9]` + optional group `{0,61}` matching nothing plus `[a-zA-Z0-9]` matching the second char. The regex `^[a-zA-Z0-9]([a-zA-Z0-9\-]{0,61}[a-zA-Z0-9])?(\.[...])* $` correctly handles all single-char, two-char, and max-63-char labels. The pre-existing `TestHandleRegister_MaxLengthHostname` used a 253-char single label — invalid per RFC 1123 — and needed updating to a multi-label form.
- **`applyRemoteConfig` is the service's trust boundary for dashboard config**: Any caller that has network access to the dashboard endpoint can send arbitrary integers for `SessionWarningThreshold` and `GracePeriod`. Clamping at the application boundary (same as `Config.Validate` does for the local config file) prevents out-of-range values from reaching the check loop regardless of what the dashboard returns.
- **Two-stage inline confirm avoids native `alert()`/`confirm()`**: Browser-native dialogs block the main thread, are not accessible to screen readers in all implementations, and look inconsistent with app styling. A `data-confirm="1"` attribute on the button toggles its appearance/label; a `setTimeout` auto-cancels if the user doesn't follow through. Error feedback goes to a `role="alert"` toast div that auto-dismisses, keeping the keyboard focus where it is.
- **CRLF drift on Windows**: `gofmt` produces LF-only output. On Windows with `core.autocrlf=true`, files checked out as CRLF will re-appear as needing formatting on every clone. Periodically run `gofmt -w .` to normalise; the CI `just lint` step catches any remaining drift.
- **Client-side URL validation with `new URL()`**: Wrap `new URL(s)` in a try/catch and check `.protocol === "http:" || "https:"` — this validates both parse correctness and scheme in one step. The server already strips invalid schemes via `Validate()`, but showing the error before the network round-trip is better UX.
- **`classifyState` extraction boundary**: The helper lives in `check.go` (same `svc` package) so `handler.go` can call it without a new file. Both files carry `//go:build windows` — no import changes needed since `fmt` and `time` are already present.
- **`setNotifyTarget` pattern**: Find-or-create by type + save + log is a stable pattern for single-type CLI setters. Parameterising by `typ string` keeps it to one function; callers pass `"webhook"` or `"ntfy"`. The log key is derived as `typ+"=disabled"` / `typ+"_url=%q"`.
- **Settings modal refresh-on-open**: Calling `loadNotifyConfig()` in `toggleSettings` whenever `isOpen` ensures the form always shows current server state, not the page-load snapshot. The cost is one extra API call per modal open — acceptable for an admin tool.
- **Auto-dismiss with guard**: `setTimeout(() => { if (st.className === "settings-status ok") { clear } }, 4000)` guards against clearing a subsequently-shown error message. Comparing `className` is safer than using a flag variable.
- **golangci-lint errcheck in tests**: Test files are subject to `errcheck` — `json.NewDecoder(w.Body).Decode(&resp)` without capturing the error triggers a warning. Wrap in `if err := ...; err != nil { t.Fatalf(...) }` to both check the error and make test failures descriptive.
- **Health API staleness must match UI staleness**: The dashboard UI marks servers offline after `STALE = 600 000 ms` (10 min). The health API must apply the same threshold to `ServerInfo.LastSeen` so API consumers get accurate counts. Otherwise a server that stops reporting remains counted as "healthy" indefinitely. Add a `staleThreshold` constant and check `time.Since(s.LastSeen) > staleThreshold` before the status switch. Explicit `case "Healthy"` is cleaner than `default:` — it avoids silently promoting unexpected status values.
- **localStorage filter persistence pattern**: Save `activeFilter` in `activateFilterPill()` and search text in `filterGrid()`. Restore with an IIFE after the pill wiring runs. The restore must call `activateFilterPill(pill)` (not just set `activeFilter`) to keep the pill's `.active` class and `aria-pressed` in sync.
- **OS dark mode live update**: The `<head>` inline script only runs once to restore a manual preference; the `@media (prefers-color-scheme: dark)` CSS handles initial auto-theme. Add a `matchMedia(...).addEventListener("change", ...)` listener to catch runtime OS changes. Guard with `!document.documentElement.dataset.theme` so manually-chosen themes are not overridden. The chart must be re-rendered because uPlot reads CSS custom properties at draw time — it does not react to DOM mutations.
- **`atomic.Pointer[T]` for cross-goroutine config handoff**: When a struct field is written by one goroutine and read by another with no other synchronisation point between them, use `atomic.Pointer[T]` rather than a bare `*T`. Embedding the `atomic.Pointer` directly in the struct avoids a separate mutex and keeps the critical section implicit. Call `Store(&value)` on write and `Load()` on read. Note that `T` itself must not be mutated after the pointer is stored — treat stored values as immutable snapshots.
- **`Remove()` must clean up all derived state**: When a registry-pattern struct has multiple per-key data structures (e.g., `servers map` and `history map`), all of them must be pruned in `Remove()`. Forgetting to delete from one causes memory leaks for hosts that are repeatedly registered and removed. Adding a test that calls `HostHistory` after `Remove` and asserts `len == 0` is the minimal regression guard.
- **`ComputeStateDurations` semantics**: The first observation in a new mode always has duration = 0 (the clock resets at the transition point). Only subsequent same-mode observations accumulate time. Tests that assert a non-zero duration for a transition record will fail — the correct test structure is: oldest (mode A, dur=0), transition point (mode B, dur=0), later in mode B (dur = elapsed since transition).

- **`pipeConn` deadline forwarding**: `pipeConn` wraps `os.File` via `os.NewFile`. Storing a deadline in a struct field without calling `c.file.SetDeadline(t)` is a silent no-op. On Windows, pipes created with `FILE_FLAG_OVERLAPPED` register with Go's IOCP poller and support `os.File.SetDeadline`. Client-side handles opened without the overlapped flag may return an error from `SetDeadline`; callers already ignore it with `_ =`, so forwarding is safe and upgrading.
- **Pipe tests via `net.Pipe()`**: `handlePipeConn` takes `net.Conn` (interface) so `net.Pipe()` provides a synchronous in-memory full-duplex connection suitable for unit testing without creating a real named pipe. Start the handler goroutine, write the request from the client end, read the response. `net.Pipe()` supports `SetDeadline`, so the 5 s timeout set by `handlePipeConn` is honoured in tests.
- **`innerHTML` NaN-date fallback**: Any `innerHTML` assignment that falls back to a raw server-provided string on parse failure must escape it. The pattern `const ts = isNaN(d) ? e.time : d.toLocaleString(...)` was missing `esc()` on the NaN branch. Even when server data is always valid RFC 3339 in practice, unescaped fallbacks violate the defence-in-depth principle applied everywhere else in the dashboard.
- **JSON decode failure also prevents connection reuse**: `json.NewDecoder(body).Decode(&v)` consumes some but not necessarily all bytes. If `Decode` returns an error (e.g., malformed JSON), the remaining body bytes are unread. Go's `http.Transport` requires the body to be fully consumed before it can return the connection to the pool — the same constraint as non-2xx bodies. Always add `_, _ = io.Copy(io.Discard, resp.Body)` on the decode-error path too, even though the 2xx status already guarantees a `defer resp.Body.Close()` will run.
- **`<link rel="icon">` type attribute must match server Content-Type**: Browsers mostly ignore this attribute in practice but monitoring tools and CSP scanners may flag mismatches. If the embedded file is a PNG, serve it as `image/png` and set `type="image/png"` in the HTML; don't claim `image/x-icon` just because the URL ends in `.ico`.
- **`json.Marshal` rejects IEEE 754 NaN/Inf**: `json.Marshal(map[string]any{"x": math.NaN()})` returns `json: unsupported value: NaN` — useful for testing the marshal-error path in functions that accept a `map[string]any` payload without needing to change the function signature or introduce an unmarshallable custom type.
- **`io.ReadAll` error testing via custom `io.Reader`**: Define `type errReader struct{}; func (errReader) Read([]byte) (int, error) { return 0, fmt.Errorf("...") }` and pass it to `httptest.NewRequest`. The `httptest` infrastructure accepts any `io.Reader`; when `io.ReadAll` is called on the request body it immediately returns the error, exercising the failure branch without any OS-level I/O.
- **Single-resource REST endpoint pattern**: `GET /api/v1/servers/{host}` complements `GET /api/v1/servers` (collection) and `DELETE /api/v1/servers/{host}` (delete). The Go 1.22 `ServeMux` longer-match rule ensures `GET /api/v1/servers/{host}` only fires when a host segment is present; `GET /api/v1/servers` continues to match the bare path. `ServerState.Get()` returns a value copy (`*info`) so the caller cannot mutate internal state.
- **disconnected_sessions are load-relevant even without a cap**: When `max_sessions == 0` (unlimited), `utilization_pct` is always 0, so the capped-gauge branch is skipped. Disconnected-but-not-logged-off sessions still hold memory and may hold session licenses. The uncapped display should always include `disconnected_sessions` alongside `active_sessions` so admins have the full picture.
- **`Retry-After` header on token-bucket 429**: `Allow()` returning `(bool, time.Duration)` is cleaner than a separate `WaitTime()` method because it keeps the deny decision and the associated delay co-located. The wait is `ceil((1 - tokens) / rate)` seconds — always ≥ 1 s since `tokens < 1` and rate > 0. `rateLimitMiddleware` converts the duration to a whole-second string and sets `Retry-After`; the `int(wait.Seconds())` cast truncates, but `math.Ceil` already rounded up so the result is exact.
- **400 vs 500 for input validation errors**: `UpdateNotifySettings` can fail for two distinct reasons — a local I/O error (config file unreadable → 500 is correct) or an out-of-range value supplied by the caller (→ 400). The handler is the HTTP boundary; it must validate HTTP input and map validation errors to 4xx before delegating to the domain function. Duplicating the range check in the handler is correct: the domain function enforces invariants, the handler enforces the HTTP contract.
- **HTTP response body must be drained for connection reuse**: Go's `http.Transport` does not reuse a keep-alive connection unless the response body is read to completion before `Close()`. `defer resp.Body.Close()` alone is not sufficient — add `_, _ = io.Copy(io.Discard, resp.Body)` before returning to drain any unread bytes. This applies to every outbound HTTP call that cares about connection pool efficiency (notifications, config fetches, health probes).
- **`AuditStore.Prune` vs `MemAuditStore.Prune` consistency**: Both rewrite kept records to a file; both should handle `json.Marshal` errors identically (skip the record). `AuditRecord` contains only primitive-typed fields so Marshal never actually fails, but silently writing an empty line on error is wrong — `scanRecords` would skip it on re-read, silently dropping the record. The correct pattern matches `MemAuditStore`: `if err != nil { continue }`.
- **`Config.Validate` must reject unknown `NotificationTarget.Type`**: Stripping URL-scheme violations is not enough — an unrecognised `Type` (e.g. `"email"` or `""`) passes URL validation but falls through the `switch target.Type` in `SendNotification` without firing or logging, creating a silent black hole. Strip at the same `Validate()` call site where URL schemes are validated, so the guard applies uniformly to every save path (config file, dashboard API, `UpdateNotifySettings`).
- **`ClassifyState` belongs in the root package**: The function is pure domain logic (`bool, time.Duration, time.Duration → string, string, int`) with no platform dependencies. Keeping it in `internal/svc` made it inaccessible to the root `Check()` function, which duplicated the same three-way if/else. Promoting it to the root package with an exported name eliminates the duplication and makes it independently testable. The root `Check()` uses `effectiveDur` — audit-store `StateSince` duration when available (more precise), falling back to `time.Since(KeyModified)`. When `KeyModified.IsZero()` (timestamp unavailable), `stateDur=0` ensures `ClassifyState` conservatively returns Grace rather than Alert.
- **History modal transition field**: `CheckResult` already carries `transition bool` and `transition_from string` in its JSON serialization. The modal JS only needed to check `e.transition` and render differently — no API changes, no backend changes. The CSS left-border approach (`border-left: 3px solid var(--accent); margin-left: -3px`) avoids `color-mix()` which lacks broad browser support and keeps the highlight contained to the entry row without affecting layout.
- **`negotiateRequest` 401 body drain**: The 401 response path called `resp.Body.Close()` without draining first. On HTTP/1.1 keep-alive connections this prevents the transport from reusing the connection for the retried (authenticated) request. Drain with `_, _ = io.Copy(io.Discard, resp.Body)` before `Close()`. The same applies to any non-decode response path — `ReportState` and `RemoveServer` success returns did not drain either.
- **Service registration retry pattern**: The startup registration attempt is best-effort — the dashboard may not be reachable when the service starts. The comment "will retry on report" in earlier code was aspirational: `ReportState` returns 403 for unregistered hosts, so if startup registration fails the service is effectively silenced. Fix: track `dashRegistered bool` in the Execute loop and retry on every poll tick until it succeeds. Extract registration + auto-pin into `registerWithDashboard` so the retry path is identical to the startup path.
- **`handleHistory` fetch granularity**: When `changesOnly=false`, the requested `limit` should be passed directly to `HostHistory()` — there is no reason to allocate 100 records if the caller asked for 5. When `changesOnly=true`, the full ring is still needed so filtering can see all transitions before trimming. The asymmetry is intentional: filtering is applied in the handler, not in `HostHistory()`, so the fetch size is the only lever available at the handler level.
- **`Config.Validate` symmetric range guards**: Every numeric field validated in `UpdateGracePeriod`, `UpdateSessionThreshold`, and `handlePutNotifyConfig` must also be guarded in `Validate()`. If `Validate()` is narrower (e.g., only checks lower bound), hand-edited config files can produce out-of-range values that bypass all the API-level guards. The pattern: `< min || > max → default` in `Validate()`, `< min || > max → error` in API handlers.
- **`atomic.Pointer[T]` does not protect the pointed-to value**: `handler.cfg.Store(&cfg)` establishes a happens-before relationship so that all writes preceding the `Store` in goroutine A are visible to goroutine B after a matching `Load`. But it only does this if `Store` is actually called. Calling `applyRemoteConfig(remote, &cfg, ...)` mutates `cfg` in place — without a subsequent `handler.cfg.Store(&cfg)`, the mutation is not released to the pipe handler goroutine. Every write-then-publish pattern requires the atomic store even when storing the same pointer address.
- **nil slice vs empty slice in JSON API**: `json.Marshal([]T(nil))` produces `null`; `json.Marshal([]T{})` produces `[]`. API consumers defensively guard with `|| []` in JS, but the server should return a correct JSON array regardless. Normalise nil to empty slice before encoding when the field semantically represents a list.
- **`max_sessions=0` means unlimited, not zero**: `ReadMaxSessions` returns 0 when `MaxInstanceCount` is absent from the registry (unlimited sessions). UI code that shows `N/max` must guard `max > 0` before including the denominator — showing `sess=3/0` implies an error rather than unlimited capacity.
- **`Cache-Control: no-store` belongs on authenticated API responses**: Setting it in `securityMiddleware` is cleaner than per-handler because it applies uniformly. Handlers that intentionally cache (favicon: `public, max-age=86400`) or revalidate (UI: `no-cache`) call `w.Header().Set("Cache-Control", ...)` after the middleware sets the default, so the last write wins and the override takes effect correctly before the first write to the body.
- **Guard `max_sessions == 0` in every display context**: `MaxInstanceCount` absent from the registry → `ReadMaxSessions()` returns 0 (unlimited) → `UtilizationPct` is 0. Any display path that formats `N/max` or `N% utilization` must guard `max > 0`. Pattern: server card, history modal, and event log all need the same guard — if you fix it in one place, check the others.
- **Session-warning cooldown must reset on recovery**: `LastSessionWarnNotify[url]` is set when a `session_warning` fires. With `RepeatMinutes=0` (fire once), the entry is never cleared — not even when sessions drop below threshold. The fix: call `resetSessionWarnCooldown` on every check cycle where utilization is below threshold so the warning fires again when utilization rises next time. `pruneNotifyState` only removes entries for deleted targets; a separate reset is needed for the "recovered" case.
- **`AbortController` for sequential async UI operations**: When a UI action (e.g. toggling "Transitions only") can interrupt a pending fetch, use `AbortController` to cancel the stale request. Pattern: `if (_ctrl) _ctrl.abort(); _ctrl = new AbortController(); fetch(url, { signal: _ctrl.signal })`. In the catch block, `if (e.name === "AbortError") return` so aborted requests don't show an error to the user. Also abort in the close handler so in-flight requests don't update DOM after the modal is dismissed.
- **Pre-clear repeat timers before reassigning**: When a timer ID is stored in an object (e.g. `_rmTimers[h] = setTimeout(...)`) and the timer can be re-triggered before it fires (e.g. after a grid re-render), always call `clearTimeout(_timers[key])` before setting the new timer. Without it, the old timer's cleanup callback (`delete _timers[key]`) runs and removes the new timer's reference, making it uncancel­able.
- **`title` alone is not sufficient for screen reader labels**: Interactive elements (buttons) must have an `aria-label` or visible text that screen readers will announce. `title` is advisory and not reliably announced, especially on mobile or in focus mode. Add `aria-label` whenever a button's only accessible name comes from a `title` attribute.
- **Event log transition arrow direction**: `prev → next` is the conventional order. Mixing an arrow with the word "from" (`"status=Healthy → from Alert"`) creates a double reversal that confuses direction. Use `stLabel(prev) + " → " + stLabel(next)` and omit the `status=` prefix — the event row's severity colour already communicates the new status.
- **Async loading state must be cleared on all failure paths**: When a JS function shows a "Loading…" placeholder and then performs an `await`, every early-return path (including `if (!r.ok) return`) must either populate the UI or replace the placeholder with an error message — a bare `return` leaves the spinner/message visible forever since there is no timeout. Apply the same discipline to `catch` blocks: `console.error(...)` alone is invisible to the user.
- **Type-switch URL field clearing**: A settings UI with multiple mutually-exclusive types (ntfy topic vs webhook URL) must consider what happens to the URL field when the user switches types mid-entry. An ntfy topic name (`my-alerts`) is not a valid webhook URL — preserving it on type switch causes a confusing red-border validation error at save time. The right heuristic: if the current value doesn't look like the new type's expected format, clear it. The reverse direction (ntfy stripping `https://ntfy.sh/`) was already handled.
- **AbortController scope matches the modal lifecycle**: `loadHistory` aborts on close and on each new load call. The same pattern applies to any fetch triggered by modal open events — `loadNotifyConfig` needs an abort on close (`toggleSettings` else branch) and a cancel-and-restart on each open. Store the controller in a module-level variable alongside the other modal state variables; null it after abort in the close path.
- **Dismiss timers must be cancelled before reassignment**: A `setTimeout` that auto-clears a status message is harmless if only one save can be in flight at a time. But if the user saves twice in rapid succession, the first timer fires after 4 s and clears the second save's message. Fix: store the timer ID, cancel it at the top of the success branch, then set a new one. The `className` guard already prevents clearing an error — this prevents clearing a stale timer's success message.
- **Captured DOM references in setTimeout callbacks become stale on re-render**: A `const btn = ...` captured in a closure and used 5 s later may refer to a detached DOM node if the grid refreshed in the interim. Re-query the element inside the callback using a stable selector (e.g. `[data-host="..."]`) rather than relying on the closure. The captured `h` (hostname string) is stable; the DOM node is not.
- **`SendTestNotification` test surface**: The function accepts `[]NotificationTarget` and iterates, dispatching each via the same `sendWebhook`/`sendNtfy` helpers as `SendNotification`. Tests can use `httptest.NewServer` for both types. The "no targets" and "empty URL" cases are the only two early-return paths; all other tests exercise the full dispatch loop. Verify that `event=test` is in the webhook payload and that multiple targets each receive exactly one call.
- **Font consistency across embedded assets**: When a Google Fonts link is trimmed (removing an unused font family), every CSS `font-family` reference in the same file must be audited and updated. A stale reference produces a silent browser fallback — the element renders but uses the browser's default serif/sans-serif instead of the designed typeface. Pattern: before removing a font from the `<link>`, run `grep "FontName"` across the file to confirm zero CSS references remain.
- **`ToServiceConfig` and `ToDashboardConfig` unit conversions**: `Config.GracePeriod` is stored as `int` (minutes) in JSON but consumed as `time.Duration` by the service. `ToServiceConfig` multiplies by `time.Minute`; `ToDashboardConfig` converts `AutoPin *bool` to `bool` (nil → false). These conversions are trivially correct but worth unit-testing because an accidental swap (e.g., `time.Second` instead of `time.Minute`) would produce a 60× error in the grace period without any compile-time failure.
- **Body drain coverage must include error paths**: `io.Copy(io.Discard, resp.Body)` is required before any early-return that does not read the body (e.g., non-2xx status checks). If the body is only drained in the success path (via `json.Decode`) and not in error paths, failed requests prevent connection reuse. Audit every `negotiateRequest` caller and every direct `dashClientPtr.Load().Do()` call. The rule applies even to paths that are relatively rare (e.g., dashboard returning 403/500) since those are exactly the paths taken during failure modes when connection pool pressure is highest.
- **`http.NewRequest` failure ≠ `http.Do` failure**: Two distinct error paths exist in functions that call `httpClient.Do`. An invalid URL (null byte) fails at `http.NewRequest`; a valid but unreachable URL fails at `httpClient.Do`. Tests must cover both paths separately. The pattern for testing `Do` failure: create an `httptest.NewServer`, capture its URL, call `srv.Close()`, then call the function — connection-refused fires immediately and covers the `Do` error branch without slow network timeouts.
- **Token-bucket cap clamp requires a large time gap to trigger**: The `if b.tokens > rl.cap { b.tokens = rl.cap }` guard requires that refill produces more tokens than the bucket cap. With `rate=10, cap=5`, backdating by 1 second adds 10 tokens to any non-zero starting balance, easily exceeding cap. The existing refill test used `rate=100, cap=1, backdate=10ms` which adds exactly 1 token — matching cap, not exceeding it. Always back-date by more than `cap/rate` seconds to reliably trigger the clamp.
- **Testing I/O error branches in `save()` on Windows**: Creating a directory at the file path blocks `os.WriteFile` (can't create a regular file where a directory exists); creating a directory at the rename target blocks `os.Rename`. These are the two reliable I/O-error scenarios available without mocking or OS-level permission manipulation. `NewServerState` constructs its path as `filepath.Join(dataDir, "servers.json")` — pass `dataDir` (not the full path) to `NewServerState`, and place the blocking directory at `filepath.Join(dataDir, "servers.json.tmp")` or `filepath.Join(dataDir, "servers.json")` respectively.
- **`os.Rename` failure via exclusive Windows file handle**: `MoveFileEx(src, dst, MOVEFILE_REPLACE_EXISTING)` requires the ability to delete the destination. Opening the destination with `windows.CreateFile(..., windows.FILE_SHARE_READ, ...)` (omitting `FILE_SHARE_DELETE`) prevents deletion and forces the rename to fail with a sharing violation ��� the cleanest way to exercise rename-error branches in `Prune` and similar rewrite-then-replace operations without needing a read-only filesystem or disk-full simulation.
- **`t.Setenv` for environment-dependent fallbacks**: Use `t.Setenv("VAR", "")` to test fallback code that triggers when an env var is absent. The testing package restores the original value automatically at test cleanup, making it safe in parallel and sequential test suites alike. Prefer this over `os.Setenv` + manual defer, which can leave state behind if the test panics.
