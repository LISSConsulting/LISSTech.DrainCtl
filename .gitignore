# Build artifacts
dist/
*.msi

# Frontend build outputs (generated by Vite — rebuild with `just frontend`)
frontend/dist/
internal/dashboard/dist/

# Node modules
frontend/node_modules/

# Go
*.exe
*.dll

# Generated from cmd/drainctl/drainctl.rc.tmpl + scripts/version.ps1 by `just resource`
cmd/drainctl/drainctl.rc
cmd/drainctl/drainctl.syso

# WiX
installer/obj/
installer/bin/

# Message compiler intermediates (keep .mc source and .dll output)
assets/*.h
assets/*.rc
assets/MSG*.bin
assets/*.o

# Environment
.env

# IDE
.vs/
.vscode/
.idea/
*.swp
*~

# Coverage / test output
*.out
*.cov
cov_*.html
dash_*.html
pipe_*.html
root_*.html
prune_*.html

# Tool state
.ralph/
.superpowers/
.playwright-mcp/
.claude/settings.local.json
.claude/worktrees/

# Local MSI revision counter (per-commit; bumped by `just all`/`just release`)
.msi-revision.json

# Ephemeral working directory (patches, scratch output, coverage runs)
.tmp/

# Scratch / temp files
MSG00409.bin
drainctl.h
drainctl.rc
